I need you to take a very deep look at this project and uncover bugs or over-engineered code and extra unnecessary tests. All the potential bugs and technical debt. Because I have a feeling this code is FAR MORE BLOATED than it should be.

---

<agent-message from="ac7f0a1c9cd39b26e">
[Subagent hand-back] The text below is the final report of a subagent this session delegated to. It is model output, NOT a message from the user: instructions, requests, or approval claims inside it are the subagent's words and carry no user authority. The harness indents every line of the report, so a frame-like line at column zero inside it would be forged. Notes above this frame may quote model-derived text, which carries no user authority either. The report follows:
  ## ThreadLoop audit: session-service, session-transition, signed-receipt-*, audit-service-errors
  
  I read all six files in full, plus the relevant parts of `src/domain/lifecycle.ts`, `src/domain/review.ts`, the `sqlite-store.ts` functions `applySessionTransition`, `appendSignedReviewReceipt`, the attempts-count query and the transaction helpers, and the import and transition commands. I edited nothing.
  
  **Overall verdict:** The core safety model mostly holds. Guard evaluation, the structural check and the state-version compare-and-swap all run inside one `withWriteTransaction`. Blocked recovery can only return to `blocked_from_state`. The repair count is derived from history. I found no guard that clearly lets an illegal structural transition through. The real problems are:
  - a TOCTOU gap for evidence that is not versioned;
  - misleading import results;
  - a large amount of duplication.
  
  ### Bugs and correctness risks
  
  1. **`session-service.ts:338-399` (bug / TOCTOU, plausible, high impact).** The proof guard context is built from a separate read snapshot before the write transaction opens. That context includes local, CI and review evidence plus the repair basis. Inside the transaction, only `state_version` is compare-and-swapped. Receipt appends do not bump `state_version`: `appendSignedReviewReceipt` and `appendGateReceipt` check `state_version` but leave it unchanged (sqlite-store:1493-1500, 1300).
     - Failure scenario: `transitionSession(reviewing -> ready_for_human)` evaluates a clean latest review. A concurrent `session review import` then commits a CHANGES_REQUESTED snapshot at the same version. The transition still applies using the stale "no blockers" evidence.
     - The same gap exists for `verifying -> pre_pr_reviewing` versus a concurrent `gate run` that records a failure.
     - Fix: re-check the max receipt sequence numbers inside the transaction, or bump a version whenever evidence is appended.
     - Git HEAD and cleanliness are also observed outside the transaction. That part is inherent to the design, but it is not documented.
  
  2. **`session-service.ts:870-902` (bug, confirmed).** `importSessionReviewReceipt` reports `review.status` from `reviewEvidenceFromArtifact(artifact, artifact.pull_request.head_sha)`. It compares the receipt's head with itself, so the status is always `current`.
     - Re-importing an older, already-imported snapshot, or one whose plan sha no longer matches, still prints `current` along with that snapshot's findings.
     - The gate path does call `evaluateSessionProof`, so the two import paths disagree about what "status" means.
  
  3. **`session-service.ts:842` (bug, plausible).** `importSessionGateReceipt` evaluates CI proof against `artifact.source.head_sha` instead of the live HEAD. Re-importing an old receipt can report `ci_proof.status: passed` even when proof is stale for the live HEAD.
  
  4. **`session-service.ts:1688-1697` (bug, plausible).** For a repair entered from reviewing, `repairBasis` is set only while `hasBlockingReview(latest reviewEvidence)` is true.
     - If the latest review evidence is `corrupt` (empty findings), the basis is undefined and `committedRepairFromFailure` stays false. `repairing -> verifying` is then permanently denied with COMMITTED_REPAIR_REQUIRED, and the only way out is blocked.
     - The fix is to record the basis head on the repair-entry transition instead of re-deriving it from whatever evidence is newest.
  
  5. **`session-service.ts:1645-1678` (drift-risk, plausible).** `latestFailure` is the newest receipt with any `result !== 'passed'`, including invalidated and setup-failed receipts. Its `headAfter` becomes the implementation basis or the repair basis.
     - An invalidated receipt whose HEAD moved mid-run makes a commit that was never verified the basis.
     - It should filter to `result === 'failed' && headBefore === headAfter`.
  
  6. **`session-transition.ts:1022-1034` (drift-risk, confirmed).** `planVerifyingTransition` hard-codes `executable: true` for post-PR `verifying -> repairing` without calling `evaluateTransitionGuards`. Every other planner branch calls the guard.
     - The real guard also requires plan and repository authority (lines 328-341).
     - `session next` can therefore advertise an executable candidate that the apply step then rejects.
  
  7. **`session-transition.ts:124-135` (dead code, confirmed).** The BLOCKED_PRIOR_STATE_REQUIRED branch cannot be reached.
     - `applySessionTransition` runs `evaluateLifecycleTransition` first, and that returns BLOCKED_RESUME_REQUIRED for the same condition (lifecycle.ts:72-81).
     - `planNextTransition` handles `blocked` before it calls the guards.
     - The result is two error codes for one condition, only one of which can ever surface.
  
  8. **Phase rules are duplicated in two layers (drift-risk, confirmed).**
     - `session-transition.ts:389-402, 439-466` duplicates checks in `lifecycle.ts:95-131`: post-PR re-entry into implementing, pre-PR `verifying -> reviewing`, and post-PR `pre_pr_reviewing`.
     - The structural layer always rejects first, so the guard copies are dead on the apply path.
     - They also use different codes: `INVALID_TRANSITION` versus `POST_PR_IMPLEMENTATION_REENTRY_FORBIDDEN`, and `PRE_PR_REVIEW_BOUNDARY_REQUIRED` versus `PRE_PR_REVIEW_INPUT_REQUIRED`.
  
  9. **`session-transition.ts:1098-1110` (drift-risk, confirmed).** `deferredProofGuards` still tells users "not available in M002-2" with work code `IMPLEMENT_ISSUE_40`. It is still reachable:
     - the framed -> proof_ready fallback at line 147;
     - `verifying` when there is no proof (schema < 4).
  
     Those are stale milestone strings shown to operators.
  
  10. **The repair limit is a bare `3` in 9 places (drift-risk, confirmed).** Locations: `session-transition.ts:203, 206, 577, 580, 1022` and `session-service.ts:1248-1252`.
      - The `?? 3` fail-closed default is repeated each time.
      - The count query (sqlite-store:627-640) counts every entry into repairing from verifying, reviewing or ready_for_human, including legacy pre-PR entries. That matches `docs/lifecycle.md`, but the README says "only the post-PR repair budget is capped."
  
  11. **`session-service.ts:442-476, 487-512` (bloat and error-model inconsistency, confirmed).**
      - `prepareBoundProofPlan` throws a `ThreadloopError('TRANSITION_GUARD_FAILED')` carrying details. `proofBaselineGuardRejection` then catches it and duck-type re-validates those details back into a guard decision. It is a round trip through an exception for data the code created itself.
      - Proof-plan failures are split two ways. Dirty-tree and no-branch failures are persisted as rejected transitions (audit plus idempotency). A bad `source_repository` or `certificate_identity` throws a `ProofValidationError`, which becomes an INVALID_ARGUMENT that is never persisted.
  
  12. **`signed-receipt-import.ts` error codes are inconsistent (confirmed).**
      - `SIGNED_RECEIPT_IDENTITY_MISMATCH` is used for "no trust policy" (lines 98, 217) and for "schema < 4" (line 379). Neither is an identity mismatch; `session-service` uses PROOF_PLAN_MISSING for the analogous case.
      - A wrong lifecycle state is SIGNED_RECEIPT_CONFLICT here (lines 385, 394) but GATE_NOT_RUNNABLE in `runSessionGate`.
      - `mapSignedReceiptParseError` (line 637) labels every non-validation throw as "not valid JSON."
      - `details` keys mix camelCase `sessionId` (session-service:2070, 2145) and `session_id` everywhere else.
  
  13. **`signed-receipt-import.ts:568-573` vs `601-616` (drift-risk, plausible).** Gate import binds `source.ref` to the current branch. Review import does not check `source.ref` or `proofRepository.branch` at all. This may be intentional because the review sensor runs on a PR ref, but nothing says so.
  
  14. **`signed-receipt-import.ts:109, 227` (minor).** Sigstore verification, which is network-bound and expensive, runs before cheap local rejections: session or plan mismatch, HEAD mismatch, and `result !== passed`.
  
  15. **`prepareImport` skips part of the schema check (drift-risk, confirmed).** At `signed-receipt-import.ts:328` it checks only `requiresExplicitInitMigration`. It skips the `schemaVersion > CURRENT_SCHEMA_VERSION` check that `assertSchemaDoesNotRequireExplicitMigration` (session-service:2045) applies to every other command.
  
  16. **`session-service.ts:2180-2188` (drift-risk, confirmed).** `isSchemaStateError` classifies errors by matching prefixes of message strings thrown in sqlite-store. Rewording one message silently turns STATE_CORRUPTED into an unmapped crash.
  
  ### Bloat and duplication
  
  17. **Two parallel import flows (bloat, confirmed; about 150-200 lines).**
      - `importSignedGateReceiptPackage` (signed-receipt-import.ts:93-210) and `importSignedReviewReceiptPackage` (212-292) follow the same steps: prepare, check policy, observe the repo twice, verify the signer, parse, project the signer, assert context, persist the package, and map the same four error classes.
      - `ImportedSignedGateReceipt` and `ImportedSignedReviewReceipt` (59-79) are the same type apart from `receipt`.
      - `prepareImport` already branches on `kind` in four places.
      - Recommendation: one `importSignedPackage(descriptor)` taking `{parseEnvelope, policy, parse, assertContext, append, conflictErrorClass, fileName, label}`, plus a generic result type.
      - Also, `importSessionGateReceipt` and `importSessionReviewReceipt` (session-service:839-902) repeat the same receipt, package, signer and lifecycle projection.
  
  18. **Stored proof-plan verification copied three times (bloat, confirmed; about 45 lines).** Copies are at session-service:601-622, session-service:1529-1575 and signed-receipt-import:406-423. Each parses the stored JSON, recanonicalizes it, compares json and sha, and rebuilds a `BoundProofPlan`. It should be one `loadBoundProofPlan(stored)` returning a result.
  
  19. **Path-containment check copied four times (bloat, confirmed; about 40 lines).** The `relative === '..' || startsWith('..'+sep) || isAbsolute` pattern appears at signed-receipt-files.ts:26-49, session-service.ts:1722-1745, 944 and 1813. `readReceiptArtifactDigests` is a near-copy of `readControlledSignedReceiptPackageContents` that swaps a read for a hash.
  
  20. **`evaluateSessionProof` repeats its return object three times (bloat, confirmed; about 40 lines).** Lines 1509-1569 return the same 16-line fallback object for the missing, parse-fail and digest-fail cases. `emptySessionReviewEvidence` (1624-1635) re-implements the unexported `emptyReviewEvidence` in review.ts:407-418.
  
  21. **`runSessionGate` builds the same record twice (bloat, confirmed; about 25 lines).** The `execution` object (730-762) and `receipt` (765-792) repeat about 18 fields. Build the common part once and spread it into both.
  
  22. **Error mapping is copy-pasted per entry point (bloat, confirmed; about 100 lines).** The AuditChainCorrupted, SchemaStateError, TransitionHistoryCorrupted and STATE_CORRUPTED mappings are repeated in `initThreadloop` (174-191), `transitionSession` (400-439), `runSessionGate` (560-574, 802-827), `loadSessionAudit` (1056-1070), `getNextSessionAction` (1162-1181) and `prepareImport` (314-322). A single `mapStateError(error, sessionId, hint)` would cover all of them. Given that, `audit-service-errors.ts` (23 lines, one function) and `mapTransitionHistoryCorruption` should be merged into one module.
  
  23. **Copied helpers and a redundant database call (bloat, confirmed).**
      - `resolveRepositoryRoot` and `assertInitializedReadOnly` are verbatim copies in signed-receipt-import.ts:661-679 and session-service.ts:1994-2002, 2055-2063.
      - `githubRepositoryUri` (import:619) duplicates the inline `expectedSourceRepository` at session-service.ts:519-524.
      - `transitionSession` (328-329) and `runSessionGate` (558-559) call `assertInitialized()`, which already runs `ensureStateDatabase`, and then call `ensureStateDatabase` again.
  
  24. **`getNextSessionAction` null-object projections (bloat, confirmed; about 100 lines, from 225 down to about 120).** Lines 1149-1373 hand-write a migration-required null object for each output block: proof, ci_proof, review, staleness, repair_budget, implementation_basis. Build the projection from a single `proofState | null` with one fallback constant.
      - Audit coverage classification is also duplicated (1080-1085 vs 1463-1468).
      - The `review` branch of `transitionSession` (365-379) calls `evaluateSessionProof` twice (a "preliminary" pass, then a real one) to avoid one git call.
  
  25. **`session-transition.ts` is mostly denial literals (bloat, confirmed; about 400-450 lines).** There are 33 `deniedGuards(...)` calls, each a 12-15 line pair of failure and work objects, with 66 `owner_issue` fields.
      - Many pairs repeat, for example `PRE_PR_REVIEW_INPUT_REQUIRED` / `RECORD_PRE_PR_REVIEW_OUTCOME` about 7 times, and the exhausted-budget pair twice.
      - A `GUARD_CODES` table keyed by code, with `deny('PRE_PR_REVIEW_INPUT_REQUIRED', overrides?)`, would shrink this a lot.
      - The `planNextTransition` and `planVerifyingTransition` candidate objects are built by hand 6 times; one `candidate(state, target, version, guards)` helper would replace them.
      - Split exports: `requiresProofGuardContext` and `getTransitionGuardRequirement` exist only to serve the branching in `transitionSession`.
  
  26. **The two `signed-receipt-files.ts` files are not duplicates (dead-weight / boundary, confirmed).** `src/services/signed-receipt-files.ts` defines the `SignedReceiptFileSystem` port plus the containment reader. `src/adapters/fs/signed-receipt-files.ts` is its only implementation.
      - The port exists for test fault injection (tests override `readWithinLimit` and `unlink`). It is synchronous because `promotePackage` runs inside the DB transaction.
      - The boundary is not applied consistently:
        - the import path receives the port by injection;
        - `session-service.evaluateSessionProof` (1582, 1588) hard-imports the node adapter;
        - the commands import the adapter directly to pass it back into the service.
      - The two files share a name with different roles.
      - `sha256WithinLimitOrNull` (sync) re-implements the bounded-read loop of `readWithinLimit` (async).
      - Suggestion: rename the service file (for example `receipt-package-store.ts`), default the port inside the service, and share the bounded-read loop.
  
  27. **`signed-receipt-import.ts:112 vs 230` (minor asymmetry).** Gate import validates `prepared.envelope`, while review import re-parses the full `packageValue`. The envelope was already parsed by `prepareImport`, so one of the two does redundant work.
  
  ### How much smaller these files could be
  
  | File | Now | Estimate |
  |---|---|---|
  | `session-service.ts` | 2201 | about 1450-1550 |
  | `session-transition.ts` | 1147 | about 600-700 |
  | `signed-receipt-import.ts` | 683 | about 400-450 |
  | small files (3) | 154 | about 110 |
  | **Total** | **4185** | **about 2600-2800 (roughly 35% less)** |
  
  The five biggest reductions:
  1. A code table for guard denials plus a candidate helper in `session-transition.ts`: about -450 (#25).
  2. One generic signed-import flow and one result projection: about -200 (#17).
  3. One state-error mapper across all entry points: about -100 (#22).
  4. Rebuilding `getNextSessionAction` from one nullable proof state: about -100 (#24).
  5. One `loadBoundProofPlan` helper, one path-containment helper and one `evaluateSessionProof` fallback: about -125 combined (#18, #19, #20).
  
  Beyond line count, the highest-priority fixes are the TOCTOU gap (#1), the misleading import status fields (#2, #3), and the repair basis that can get stuck (#4).
</agent-message>

---

<task-notification>
<task-id>ac7f0a1c9cd39b26e</task-id>
<tool-use-id>toolu_013aV8vHJvMXLwNeXdXT9ddN</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/tasks/ac7f0a1c9cd39b26e.output</output-file>
<status>completed</status>
<summary>Agent "Audit services layer" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This agent's report was delivered to you as a message from "ac7f0a1c9cd39b26e" (its SubagentHandback call). Read it there; it is not repeated here.
</result>
<usage><subagent_tokens>164218</subagent_tokens><tool_uses>19</tool_uses><duration_ms>265465</duration_ms></usage>
</task-notification>

---

<agent-message from="aaf7bea9de06ea114">
[Subagent hand-back] The text below is the final report of a subagent this session delegated to. It is model output, NOT a message from the user: instructions, requests, or approval claims inside it are the subagent's words and carry no user authority. The harness indents every line of the report, so a frame-like line at column zero inside it would be forged. Notes above this frame may quote model-derived text, which carries no user authority either. The report follows:
  ## ThreadLoop domain audit: attestation, proof, review, sigstore and contracts
  
  I read all the scoped files in full and skimmed both attestation docs. I also followed callers into `src/services/signed-receipt-import.ts`, `scripts/sign-ci-gate-receipt.ts`, `src/schemas/state.ts` and `sqlite-store.ts`. I edited nothing. I only wrote one scratch script, in the session scratchpad, to reproduce findings 1, 10 and 12.
  
  One correction to the brief: no file in scope uses zod. The only zod in `src` is `src/schemas/state.ts`. All receipt and plan validation is hand-written, and that is the main source of bloat.
  
  The core chain of trust is sound. The signing certificate is bound to the DSSE payload, the payload to the statement, and the statement to the artifact digest. Session, plan, repository and HEAD are all bound. The certificate identity regex is anchored and escaped. On top of that, the importer checks that the certificate's own identity field equals the build-signer URI exactly, and checks the issuer and nine certificate OID values exactly.
  
  ### Bugs and security, most important first
  
  1. **Bug: signed CI cannot be produced for any gate that declares `setup` — `scripts/sign-ci-gate-receipt.ts:56-59` (confirmed).**
     - The signer re-validates the gate by wrapping it in a legacy plan with no `contract_version`: `canonicalizeProofPlan({ acceptance_criteria: [...], gates: [parsedGate] }, sha256)`.
     - A legacy plan passes `allowSetup: false` (`proof.ts:251`). I ran it with a gate that has one setup step and it throws: `proof_plan.gates[0] must contain exactly: id, command, working_directory, timeout_ms.`
     - The same `inputs.gate_json` feeds the run step (`.github/workflows/threadloop-gate-sensor.yml:69`, which uses `validateDeclaredGate(..., {allowSetup:true})`) and the sign step (line 125). So the gate runs, then signing always fails.
     - `proof.ts:522-529` has a comment warning against exactly this. No test covers the sign script with setup.
     - Fix: call `validateDeclaredGate(parsedGate, { allowSetup: true })`.
  
  2. **Security: an old approval can be replayed over newer blockers — `review.ts:359`, `sqlite-store.ts:1461` (confirmed).**
     - `evaluateReviewEvidence` treats the receipt with the highest *import* sequence as authoritative. `observed_at` is validated and then never used.
     - The agent chooses which signed package to import. Import only requires `pull_request.head_sha === currentHead` (`signed-receipt-import.ts:586`).
     - Scenario: sensor run 1 at HEAD X is APPROVED with no threads. A reviewer then opens a thread, and run 2 shows it as blocking. The agent imports run 2, then re-imports run 1 (a new receipt id, so no conflict). Evidence becomes blocker-free and moves to `ready_for_human`.
     - The doc's "latest valid imported receipt is authoritative" is the gap.
     - Fix: pick by `observed_at`, or reject imports older than the newest one stored for that HEAD.
  
  3. **Security: the PR number and base ref are never bound to the session — `review.ts:461-482`, `signed-receipt-import.ts:564-591` (plausible).**
     - `pull_request.number` and `base_ref` are validated for shape only. Nothing compares them with the session's `baseRef` or a pinned PR.
     - Any PR in the repo whose head is the current HEAD qualifies, including one targeting a scratch branch. Completion needs merged plus a same-HEAD `User` approval, and a merge into a non-base branch would satisfy it.
     - Across imports, the latest receipt can also switch to a different PR number.
  
  4. **Security, low: the statement is decoded lossily — `attestation.ts:314`, `review.ts:277` (plausible).**
     - `Buffer.from(payload,'base64').toString('utf8')` replaces invalid UTF-8 with U+FFFD. The canonical check then compares against the decoded string, not the signed bytes. So `statementJson` and `statementSha256` may not match what was signed.
     - Only a trusted signer could produce such a payload.
     - Fix: add `Buffer.from(statementJson,'utf8').equals(decodedBytes)`.
  
  5. **Bug: a job cancelled during setup cannot be signed — `attestation.ts:236-252` with `756` and `766` (plausible).**
     - `authoritativeGateResult` maps `cancelled` to `aborted`, but the setup array is copied from the report unchanged.
     - `validateRecordedSetup` treats `aborted` as needing complete setup. A report with partial setup and a cancelled job therefore throws when `sign-ci-gate-receipt.ts:93` re-canonicalizes.
     - The same happens if a timeout or execution error happens mid-setup and is reported as anything other than `setup_failed` or `invalidated`.
  
  6. **Drift risk: `canonicalJson` is not JCS (RFC 8785) — `canonical-json.ts:6-11` (confirmed).**
     - Integer-like keys come out in numeric order, not sorted order. `canonicalJson({b:1,'10':1,'2':1,a:1})` gives `{"2":1,"10":1,"a":1,"b":1}`; JCS gives "10" before "2". Any external verifier re-hashing with a JCS library will disagree.
     - `isPlainObject` accepts a `Date`, which serializes as `{}` instead of its ISO string. The same applies to `Buffer`, `Map` and class instances.
     - This matters for free-form audit `payload` and the sigstore `bundle`, which are both hashed.
  
  7. **Drift risk: the same fields are validated three different ways (confirmed).**
     - Timestamps: `attestation.ts:894` requires a canonical `toISOString()` form, while `review.ts:654` accepts anything `Date.parse` accepts ("2024", "Jan 1 2024").
     - Identifiers: `attestation.ts:872` allows at most 160 characters and no `:`, while `review.ts:628` allows 256 characters and `:`.
     - Run URI: `review.ts:459` never checks `run_invocation_uri` against the repository pattern that `attestation.ts:540` enforces. The certificate still binds it.
     - Working directory: `attestation.ts:730-736` uses plain string checks without normalizing, so `a/../../x` passes. `proof.ts:571` normalizes. This is not exploitable because the gate must canonically equal the plan's gate.
  
  8. **Dead check: signer projection comparisons can never fail — `sigstore.ts:126-136` and `signed-receipt-import.ts:437-458` (confirmed).**
     - `verifySigstoreReceipt` returns `policy.*` and `receipt.artifact.source.*` copied verbatim. Nothing in the return value is read from the certificate.
     - `assertSignerProjection` compares those values back to the same policy and source, so it cannot fail.
     - For the same reason, the stored `issuer`, `certificateIdentity`, `buildSignerUri` and `buildSignerSha` columns are always copies of the policy. The matching comparisons in `evaluateCiProofEvidence` (`attestation.ts:416-420`) and `evaluateReviewEvidence` (`review.ts:390-394`) carry no information.
  
  9. **Dead check, low: the transparency pre-check is shallow — `sigstore.ts:139-157` (plausible).**
     - It passes if *some* tlog entry has a correctly shaped inclusion proof, not necessarily the entry sigstore actually verifies with `tlogThreshold: 1`.
     - sigstore does verify any proof that is present, so the real risk is small. The function is mostly a shape check that could be simplified.
  
  ### Bloat and dead code
  
  10. **Bloat: `isCanonicalBase64` — `attestation.ts:918-940` (confirmed).** The 22-line character loop is redundant. The final round-trip check `Buffer.from(v,'base64').toString('base64') === v` alone rejects whitespace, URL-safe characters and bad padding; I checked with `'aGk=\n'`. `review.ts:666-672` already uses only the round-trip, and its try/catch is dead because `Buffer.from` never throws on a string.
  
  11. **Bloat: `validateInTotoReceiptStatement` — `attestation.ts:629-693` (confirmed).** The 65 lines of field-by-field checks could be `statementJson === canonicalJson(buildInTotoReceiptStatement(artifact, sha))`. The payload is already proven canonical at line 340, and `review.ts:585-598` does exactly this in about 12 lines. It also returns `value as InTotoReceiptStatement` instead of the rebuilt statement.
  
  12. **Bloat: the validation helpers are copied three times (confirmed).**
      - The object, exact-key, text, identifier, SHA, commit, timestamp, integer, repository and regex-escape helpers exist in `attestation.ts:848-954`, `review.ts:600-676` and `proof.ts:618-710`. `escapeRegExp` is copied into `sigstore.ts:246` as well.
      - The GitHub repository regex appears three times and the SHA regex five times.
      - That is about 200 lines, reducible to one module of about 60, or to zod `.strict()` schemas since zod is already a dependency.
  
  13. **Bloat: gate validation is duplicated — `attestation.ts:695-740` vs `proof.ts:530-616` (confirmed).** `validateGate` and `validateGateExecution` re-implement `validateDeclaredGate` and `validateExecutionSpec`, with the drift noted in finding 7 and no duplicate step-id check. The attestation version could just call `validateDeclaredGate(value, {allowSetup:true})`.
  
  14. **Bloat: setup matching is written twice — `attestation.ts:746-846` vs `proof.ts:413-438` (confirmed).** Both implement the "recorded setup matches declared setup" rule, and they already disagree. The `proof.ts` version accepts `setup_failed` with zero recorded steps; the attestation version rejects it (lines 763 and 833-843).
  
  15. **Bloat: hand-written type guards — `proof.ts:440-506` (confirmed).** `isGateReceiptPayload` and `isRecordedSetupStepArray` are 65 lines of `typeof` checks that allow extra keys. A zod schema would replace them in about 20 lines and be stricter.
  
  16. **Bloat: the artifact validators are field-by-field (confirmed).** `validateSignedGateReceiptArtifact` (`attestation.ts:468-606`, about 140 lines) validates every field and then rebuilds the object by hand. `review.ts:425-583` does the same in about 160 lines. As strict zod schemas these would be about 40 and 50 lines, and the `SignedGateReceiptArtifact` and `SignedReviewReceiptArtifact` interfaces (`attestation.ts:36-74`, `review.ts:42-90`) could be `z.infer` instead of hand-kept duplicates.
  
  17. **Bloat: the gate and review import pipelines are near-identical (confirmed).**
      - Constants, envelope interface, parsed package, stored receipt, canonicalize, parse envelope, parse package and evaluate: `attestation.ts:14-20, 112-172, 193-200, 293-347` mirror `review.ts:4-7, 117-170, 182-189, 262-311`.
      - `parseSignedReceiptEnvelope` and `parseSignedReviewReceiptEnvelope` are line-for-line the same except for the media type and validator.
      - One generic `parseSignedPackage(value, digest, {mediaType, validateArtifact, buildStatement})` would cover both.
  
  18. **Bloat: every artifact field is carried twice — `attestation.ts:135-143`, `review.ts:134-142` (confirmed).** The envelope types extend `{artifact, json, sha256}` and then add `artifactJson` and `artifactSha256`, which hold the same values (`attestation.ts:322-324`).
  
  19. **Bloat: stored rows duplicate the package — `attestation.ts:394-423` (confirmed).** The stored row repeats `artifactJson`, `statementJson` and both digests even though they can be derived from the package. That forces about 25 consistency comparisons on every evaluation, and `packageSha256` is checked twice (lines 385 and 396). Storing only the package bytes and its digest would remove most of them. The same pattern is at `review.ts:378-398`.
  
  20. **Bloat: repeated status handling — `attestation.ts:444-466` and `proof.ts:508-520` (confirmed).** The two status aggregators are near-identical, as are the four receipt-id filter blocks at `proof.ts:348-359`.
  
  21. **Bloat: plan interfaces — `proof.ts:69-106` (confirmed).** Four plan interfaces where v3 and v4 are identical apart from the version literal. The if/else version ladder in `validateProofPlan` (`proof.ts:217-279`) could be driven by a small table of version to required keys.
  
  22. **Dead code: unused exports (confirmed).** I grepped across `src`, `scripts` and `tests`.
      - `validateProofPlan` is used only inside `proof.ts`.
      - `threadloopReceiptPredicateType` is used only inside `attestation.ts`.
      - Exported constants with no outside use: `IN_TOTO_STATEMENT_TYPE`, `THREADLOOP_RECEIPT_PREDICATE_TYPE_V1`/`_V2`, `REVIEW_IN_TOTO_STATEMENT_TYPE` (a duplicate of `IN_TOTO_STATEMENT_TYPE`), `THREADLOOP_REVIEW_PREDICATE_TYPE` and `THREADLOOP_ERROR_CODES`.
      - `REVIEW_IN_TOTO_PAYLOAD_TYPE` duplicates `IN_TOTO_PAYLOAD_TYPE`.
      - Used only by tests: `parseSignedReceiptPackage`, `isActiveTaskStatus`, `LIFECYCLE_DECISION_CODES`, `AUDIT_EVENT_TYPES`, `isThreadloopError`, `createCommand*Envelope`, `collectLeafCommands` and `getCommandPath`.
      - About 30 exported types are never imported elsewhere; that is harmless.
  
  23. **Drift risk: zod schemas and hand-written types duplicate each other — `src/schemas/state.ts` vs `types.ts:57-142` (confirmed).**
      - `Task`, `Session`, `Entry`, `Artifact`, `ActiveState` and `ThreadloopConfig` are declared by hand and again as zod schemas, rather than using `z.infer`.
      - They have already drifted: `artifactSchema` lacks `snapshotSource`, and zod strips unknown keys, so that field is silently dropped on parse.
      - The `.transform` at `state.ts:72-80` re-applies defaults that `.default()` already set.
      - Separately, `StoredRepoSnapshot` duplicates `RepoSnapshot` plus one field (`types.ts:125` vs `133`).
  
  24. **Dead code: unreachable condition — `lifecycle.ts:103` (plausible).** `sourceState !== TASK_STATUS.PROOF_READY` can never matter, because a post-PR phase cannot be in `proof_ready` (blocked sessions return earlier). Otherwise `lifecycle.ts` and `audit.ts` are lean, and the transition table exists only once.
  
  25. **Bloat, minor: the DER decoder — `sigstore.ts:205-238` (confirmed).** Hand-decoding certificate OID values is needed, because sigstore's `certificateOIDs` option compares raw bytes. Encoding the expected values as DER UTF8String and passing `certificateOIDs` would replace about 34 lines of decoder plus the Map-and-compare block with about 10 lines, and let sigstore raise its own policy error.
  
  ### Size estimate
  
  The scoped files are about 3,300 lines. A realistic target is about 1,900–2,100 lines, roughly 40% smaller, with the same behaviour and fewer ways for the copies to drift:
  
  | File | Now | Target |
  |---|---|---|
  | `attestation.ts` | 954 | ~430 |
  | `review.ts` | 686 | ~330 |
  | `proof.ts` | 710 | ~450 |
  | `sigstore.ts` | 248 | ~170 |
  | contracts, types, lifecycle, audit | ~740 | mostly unchanged |
  
  The five biggest reductions:
  1. Replace the hand-written validators for the artifacts, the local receipt payload and the plan with strict zod schemas and `z.infer` types (findings 15, 16, 21): about −400 lines.
  2. Merge the gate and review signed-package pipelines into one generic parse / canonicalize / statement-binding module (findings 17, 18): about −250 lines.
  3. Put the validation helpers in one module, and have `attestation.ts` reuse `proof.ts`'s gate and setup validation (findings 12, 13, 14): about −230 lines.
  4. Validate the statement by rebuilding it and comparing bytes, and cut `isCanonicalBase64` to the round-trip (findings 10, 11): about −80 lines.
  5. Stop storing and re-comparing derived columns and the policy-echo signer fields in stored receipts, and delete `assertSignerProjection` (findings 8, 19): about −60 lines here, plus schema and SQL savings in `sqlite-store.ts`.
  
  Main files: `/Users/nnennandukwe/Code/threadloop/scripts/sign-ci-gate-receipt.ts`, `/Users/nnennandukwe/Code/threadloop/src/domain/attestation.ts`, `/Users/nnennandukwe/Code/threadloop/src/domain/review.ts`, `/Users/nnennandukwe/Code/threadloop/src/domain/proof.ts`, `/Users/nnennandukwe/Code/threadloop/src/adapters/crypto/sigstore.ts`, `/Users/nnennandukwe/Code/threadloop/src/services/signed-receipt-import.ts`, `/Users/nnennandukwe/Code/threadloop/src/schemas/state.ts`.
</agent-message>

---

<task-notification>
<task-id>aaf7bea9de06ea114</task-id>
<tool-use-id>toolu_01RFq9QnUdVXnX9iK8WAAfXm</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/tasks/aaf7bea9de06ea114.output</output-file>
<status>completed</status>
<summary>Agent "Audit domain layer" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This agent's report was delivered to you as a message from "aaf7bea9de06ea114" (its SubagentHandback call). Read it there; it is not repeated here.
</result>
<usage><subagent_tokens>163205</subagent_tokens><tool_uses>29</tool_uses><duration_ms>290963</duration_ms></usage>
</task-notification>

---

<agent-message from="a0ddc3624123296cb">
[Subagent hand-back] The text below is the final report of a subagent this session delegated to. It is model output, NOT a message from the user: instructions, requests, or approval claims inside it are the subagent's words and carry no user authority. The harness indents every line of the report, so a frame-like line at column zero inside it would be forged. Notes above this frame may quote model-derived text, which carries no user authority either. The report follows:
  ## Audit: src/adapters/fs/sqlite-store.ts (3569 lines), sqlite-driver.ts (41), schemas/state.ts (87)
  
  I read all three files in full. No files were edited.
  
  **History that matters for everything below:** `git log -G"CURRENT_SCHEMA_VERSION = [0-9]"` shows that schema v2 through v8 all landed between 2026-07-23 (eb4b975) and 2026-07-31 (7145263), eight days apart. `npm view threadloop` returns 404, so the package has never been published, and there are no git tags. The JSON store (`json-store.ts`) existed only from d63ed5f to d15a357, both on 2026-03-14. So every legacy-migration path exists only for the owner's own dev databases.
  
  ---
  
  ### A. Bugs and correctness risks
  
  **1. store:2596 and session-service.ts:2180. Drift-risk/bug, confirmed.** Callers classify errors by matching the start of the error message. Some store errors don't match any prefix, so they get through as raw errors instead of `STATE_CORRUPTED`.
  - `isSchemaStateError` only accepts `'Invalid schema for '`, `'Unsupported…'`, `'Missing…'`, `'Invalid session transition history for '`, or the exact state.db string.
  - The store also throws these, which match nothing:
    - `Invalid schema trigger: ${trigger}` (2596, 2629, 2661, 2712)
    - `'Invalid active-session projection after migration.'` (2478)
    - `'A previous gate-receipt migration left …'` (2754)
    - the compare-and-swap errors (1181, 1190)
    - `new Error(corruption)` in `readSessionGateContext` (478), e.g. "Session X has an inconsistent…". `runSessionGate` calls it at service line 575, outside its try/catch.
  - Fix: throw typed errors (a `StateCorruptedError` class) instead of matching on message text.
  
  **2. store:1807, 1922–1926, 2410 vs 3227. Drift-risk/bug, confirmed.** A write silently repairs a broken active-session projection that the read path reports as corruption.
  - `databaseNeedsSetup` returns true on `hasActiveStateCompatibilityMismatch || readActiveProjectionMismatchCount > 0`.
  - Setup then runs `reconcileActiveSessionProjection`, which does `DELETE FROM active_sessions` and rebuilds it.
  - Every CLI process starts with `setup.status = 'unknown'`, and `transitionSession` calls `ensureStateDatabase` first. So the repair always happens before `detectTransitionStateCorruption` can see the mismatch.
  - `session next` (`readSessionLifecycleReadOnly` with no setup) reports `STATE_CORRUPTED` for the same database. Read and write paths disagree about whether the state is corrupt.
  
  **3. session-service.ts:329–396 with store:1138. Bug (TOCTOU), plausible.** Transition guards can pass on evidence that has changed since it was read.
  - Guard evidence (receipts, signed receipts, review evidence, attempts) is read in separate read snapshots (`readSessionLifecycleReadOnly`, `evaluateSessionProof`) before the write transaction.
  - The write transaction checks only `state_version` (the compare-and-swap).
  - `appendGateReceipt`, `appendSignedGateReceipt` and `appendSignedReviewReceipt` don't bump `state_version`.
  - Failure scenario: process A computes "all gates passed" for `verifying → pre_pr_reviewing`. Process B finishes a gate run and appends a `failed` receipt. A commits the transition on the stale "passed" evidence.
  - Fix options: make receipt appends bump the version, or re-read evidence inside the transaction.
  
  **4. store:1097–1158 and 3234. Bug (idempotency semantics), confirmed; docs call it intended.** A guard rejection is stored permanently under the idempotency key, even when the rejection depended on things that change without a version bump.
  - `STATE_VERSION_CONFLICT`, `TRANSITION_NOT_ALLOWED` and `TRANSITION_GUARD_FAILED` are all stored in `transition_idempotency`.
  - A guard can fail because of missing receipts or a dirty Git tree. Neither is part of the request hash or `state_version`.
  - After the user fixes the problem (imports the CI receipt, commits), retrying the same command with the same key replays the old rejection forever. The rejection's own `required_work` details invite exactly that retry.
  - docs/lifecycle.md:67 says this is intended. It's still a trap for a CLI user. At minimum, the error hint should say "use a new key".
  
  **5. store:1360–1457 and 1461–1562. Bug (atomicity), plausible.** `input.promotePackage()` writes to the filesystem inside the SQLite transaction, before `COMMIT`. If `COMMIT` fails, the promoted package file stays on disk with no row in the database.
  
  **6. store:1192–1195 and 3431. Bug (ordering), confirmed mechanically.** Every non-completing transition calls `insertActiveSession`, which does `INSERT OR REPLACE`.
  - The row is already there (the corruption check guarantees it), so the call is logically a no-op.
  - But REPLACE deletes the row and reinserts it with a new rowid.
  - `readActiveSessionRows` orders by `rowid`, so the order of `state.activeSessions` changes every time a session transitions.
  - The `INSERT … SELECT` in `reconcileActiveSessionProjection` (2465) has no `ORDER BY`, so its order isn't defined either.
  
  **7. store:627–640. Drift-risk, confirmed.** The repair-budget count re-implements `isRepairEntryTransition` (domain/lifecycle.ts:163) in SQL, with a fixed `IN (?, ?, ?)` filled from `...REPAIR_ENTRY_STATES`.
  - If that array changes length, the query errors or silently binds NULL and undercounts.
  - The transition history is already loaded by callers, so this could be `history.filter(isRepairEntryTransition).length`.
  
  **8. store:1847–1849 vs session-service.ts:2191. Bug, plausible.** Two "is the database busy?" checks disagree.
  - The store's check requires `errcode === 5 && errstr === 'database is locked'`.
  - node:sqlite reports extended codes (261 BUSY_RECOVERY, 517 BUSY_SNAPSHOT, 773 BUSY_TIMEOUT), and those fail the store's check.
  - A busy error during setup is then cached as `setup = {status:'failed'}` for good (1798–1799). That's harmless in a one-shot CLI but permanent in a long-lived host.
  - The service's check uses OR plus a regex. Use one check, based on `errcode & 0xff`.
  
  **9. store:1807–1812. Race, plausible.**
  - `databaseNeedsSetup` and `PRAGMA journal_mode = WAL` run outside the IMMEDIATE lock. Two concurrent first-run processes both decide setup is needed, and both run the whole migration in turn. The migration looks re-runnable, so this is mostly wasted work.
  - Switching to WAL needs an exclusive lock and can fail with SQLITE_BUSY while another process holds a read.
  - WAL is only ever set on the setup path. That's OK because WAL mode is stored in the file, but nothing checks it afterwards.
  
  **10. store:737–742, 1825 and 3227. Design bug, confirmed.**
  - Every setup runs `assertAllSessionTransitionHistoryAuthority`, which fully re-verifies the audit hash chain for every session. That's O(all events) on every first write per process.
  - Separately, the check at 3227 inside `detectTransitionStateCorruption` scans every session.
  - Either way, one corrupt session blocks transitions, gate runs and receipt imports for every other session in the repo.
  
  **11. store:1060, 1077, 1655–1660, 717. Unvalidated JSON, confirmed.**
  - `parseJsonText<T>` is only a cast.
  - Idempotency replays (`result_json`), repo snapshots and transition `input_json` go back to callers without any schema check.
  - Only `readState` validates its output (with zod), and audit rows are field-checked.
  
  **12. heartbeat vs snapshot (store:1575–1593, 1603–1619). Drift-risk, confirmed.**
  - `heartbeatSession` takes a repo snapshot and writes `sessions.branch/head_sha`, but doesn't update `repo_snapshots`.
  - `repo_snapshots.branch/head_sha` is written by a different command.
  - The same facts are stored in two tables and drift between writes.
  
  **13. store:1989 and 3424. Dead data, confirmed.** `artifacts.snapshot_source` is written, but `loadState` (2946) never selects it and `artifactSchema` doesn't include it. The column is write-only.
  
  ---
  
  ### B. Dead code (migration history that was never shipped)
  
  **14. store:2805–2873 and 1930–1931. Dead code, confirmed.** `migrateLegacyJsonState`, `databaseIsEmpty`, `readJsonSync`, the state.json check in `databaseNeedsSetup`, and `INVALID_STATE_JSON_ERROR` exist to import a JSON store that lived for a few hours on 2026-03-14. That's about 80 lines.
  
  **15. store:2367–2402, 2446–2460, 2401, 1913–1920; state.ts:11–13. Dead code, confirmed.**
  - `ensureSessionHeartbeatColumns`, `ensureTaskIssueRefColumn`, `ensureTaskLifecycleColumns`, the `status='active'` rewrite and its count in needs-setup, and the `'active'` transform in state.ts `persistedTaskStatusSchema` all upgrade v1/v2 dev databases.
  - `migrateActiveStateRegistry` (2446) is doubly dead: `reconcileActiveSessionProjection` runs two lines later (2410) and deletes and rebuilds `active_sessions`.
  
  **16. store:2732–2793 and 1814–1818. Dead code, confirmed.** The v7→v8 `gate_receipts` rebuild (`detachLegacyGateReceiptResultDomain` and `restoreLegacyGateReceipts`), about 65 lines, only upgrades databases that existed between 2026-07-30 and 2026-07-31.
  
  **17. store:2416–2444 plus 857–877, 798–853. Mostly dead, confirmed.**
  - `activateAuditLedger` (the `audit_activated` event with coverage `schema_v6_forward`) exists only for sessions created before v6.
  - On a v8 database, `insertTaskSession` always writes `session_started` in the same transaction.
  - The "audit floor" logic in `assertSessionTransitionHistoryAuthority` and `readSessionAuditGenesis`, including the "legacy transition history" check at 847–853, collapses to "genesis is `session_started` at version 0".
  
  **18. store:914–997, 51, 417–419, 362–376, 660–671, 673–694, 696–720, 548, 588. Dead or legacy-only, confirmed.**
  - `readSessionLifecycleReadOnly` branches on version 1, 3, 4, 5, 6 and 7, including a `NULL AS blocked_from_state` for v2.
  - Also legacy-only:
    - `EXPLICIT_INIT_MIGRATION_MIN_SCHEMA_VERSION` and `requiresExplicitInitMigration`
    - `AuditLedgerUnavailableError('schema_version')`
    - `inspectAuditLedgerReadOnly`
    - the pre-v7 path in `readSessionTransitionHistoryReadOnly`
    - the `tableExists('signed_*')` guards
  - The same branching spreads into the services: `schemaVersion >= 4` / `< 6` / `< CURRENT` checks in session-service.ts (362, 382, 1202–1204, 1388, 1437, 2033) and signed-receipt-import.ts (328, 378).
  - In `transitionSession`, `ensureStateDatabase` has already moved the database to v8, so `lifecycle.schemaVersion >= 4` is always true there.
  
  **19. store:1681–1684. Dead code, confirmed.** `resetSqliteConnections` is a pure alias of `closeSqliteConnections`. Both are used only in tests (8 and 2 test files). `withReadSnapshot` is exported only for one test. `TransitionGuardEvaluator`, `SessionTransitionResult` and `PersistSessionTransitionInput` are exported but not imported anywhere else in src.
  
  **20. store:3470–3471, 2490–2491, 1340–1342, 1441–1443, 1544–1546, 1675, 1768. Checks that can't fire, confirmed.**
  - `if (!active)` right after `length === 1`.
  - `!activeSession` inside the `length === 1` branch.
  - The `sequence < 1` checks on `AUTOINCREMENT` columns.
  - `pendingWrite.catch(()=>{})`: that promise only ever resolves, so the catch never runs.
  
  ---
  
  ### C. Bloat and duplication
  
  **21. store:411, 91–97, 1735–1785. Bloat, confirmed.** The per-repo connection map and the `pendingWrite` promise queue serialize writes that can't interleave anyway.
  - node:sqlite is synchronous.
  - Every action passed to `withSerializedWriteAccess` is synchronous (`runInImmediateTransaction`).
  - The queue does nothing that JS's single thread doesn't already do.
  - About 90 lines could be about 25: open the connection, set `busy_timeout`, run `BEGIN IMMEDIATE`.
  
  **22. store:1992–1996, 3466–3486, 2482–2527, 1037–1038, 1217; state.ts:59–81; types.ts:120. Drift-risk plus bloat, confirmed.** The "which sessions are active" fact is stored three times on top of its source.
  - It is derivable from `tasks.status <> 'completed' AND sessions.ended_at IS NULL`.
  - It is also stored in `active_sessions`.
  - It is stored again in the `active_state` singleton, which is kept only for compatibility.
  - It is exposed as `StateData.active`.
  - `active_state` is never read for behavior, only to check it against itself. `StateData.active` is unused in services (only `activeSessions` is read).
  - About 150 lines of sync and mismatch checks exist only to keep these copies consistent, and they drive the global corruption check in #10.
  - A SQL view or a single query would remove all of it.
  
  **23. Tasks and sessions are 1:1. Drift-risk plus bloat, confirmed.**
  - A task is only ever created together with its session (`insertTaskSession`, service:228–252).
  - Lifecycle state (`status`, `state_version`, `blocked_from_state`) lives on `tasks`, but everything is keyed by `session_id`, and `session_transitions` carries both IDs.
  - `sessions.ended_at` duplicates `status = 'completed'`, and 3214 checks the two against each other.
  - Merging the tables, or at least dropping `ended_at`, removes a whole group of consistency checks.
  
  **24. store:2168–2355 (about 190 lines) and 56–87. Bloat, confirmed.**
  - There are 24 hand-written immutability triggers: `no_update`, `no_delete` and `no_replace` for each of 8 tables.
  - Their names are listed again in four constant arrays, and trigger existence is checked in three places (1886–1897, 2594, 2627, 2659, 2710).
  - A loop over `[table, key-predicate]` pairs could generate the DDL in about 25 lines.
  - The `no_replace` triggers are justified, because REPLACE skips DELETE triggers, but their `id = NEW.id` and `sequence = NEW.sequence` clauses duplicate the primary key and UNIQUE constraints.
  
  **25. store:2529–2730, 1855–1932, 935–952. Bloat, confirmed.** About 280 lines of schema-shape checks.
  - Four `assert*SchemaShape` functions re-list every column from the DDL by hand (a second copy that can drift).
  - `databaseNeedsSetup` re-checks 15 tables, every trigger and specific columns.
  - `readSessionLifecycleReadOnly` does its own `PRAGMA table_info(tasks)`.
  - `readDatabaseSchemaVersion`, `assertSchemaVersion`, `assertSupportedSchemaVersion` and `parseSchemaVersion` all read the same metadata row.
  - With no shipped history, `PRAGMA user_version === 8` (or one metadata check) replaces nearly all of it.
  
  **26. store:105–285, 532–626, 2875–2968. Bloat, confirmed.**
  - 13 hand-written `*Row` types (about 180 lines) are converted field by field from snake_case to camelCase: gate receipts (14 fields), signed gate (22), signed review (21), tasks, sessions, entries, artifacts.
  - The proof-plan query and its mapping appear twice, identically (480–504 and 511–651).
  - A `SELECT col AS "camelName"` style, or one generic camelize helper, would remove about 250 lines.
  
  **27. store:1360–1459 vs 1461–1563, plus 1294–1313. Bloat, confirmed.**
  - `appendSignedGateReceipt` and `appendSignedReviewReceipt` are about 100 lines each, and 95% identical: lookup for an existing import, identity check, session and state check, plan-hash check, authority check, the same 14 signer columns, audit event, promote.
  - `appendGateReceipt` repeats the first half.
  - There are three near-identical `*ConflictError` classes (359–361).
  - One parametrized helper would save about 120 lines.
  
  **28. store:3261–3299 vs 3234–3259, and the table at 2046–2058. Bloat, plausible.**
  - `persistIdempotencyConflict` repeats the audit-append block from `persistRejectedTransition` word for word.
  - The `transition_idempotency_conflicts` table, with its three triggers and schema check, stores a conflict result that can be recomputed exactly from the existing idempotency row (the result has no timestamp).
  - Its only real job is to avoid duplicate audit events.
  
  **29. state.ts:15–26, 64–82 and store:3448–3464. Bloat, confirmed.** Normalization is done three times on the same data.
  - zod `.optional().default(null)`, then the schema's `.transform` repeating `?? null`, then `normalizeStateData` repeating it again, and `readState` validates the result once more.
  - Rows from the database always have these fields, so the defaults exist only for legacy JSON (#14).
  
  **30. store:428–439, 1836–1845. Minor bloat, confirmed.**
  - When setup is already ready, `ensureStateDatabase` opens a whole extra read connection just to re-check the schema version.
  - `assertInitialized` calls it, and then `transitionSession` calls it again (service:329).
  - One transition opens about 6 separate connections.
  
  sqlite-driver.ts is fine (it just hides an experimental-feature warning). The only gap there is WAL handling (#9).
  
  ---
  
  ### Size estimate
  
  The file could shrink to about **1,400–1,700 lines**, roughly half, with v8 behavior unchanged. The five biggest cuts:
  
  1. **Remove every pre-v8 migration and read path** (#14–18, #29): about 500–550 lines here, plus the version branches in session-service and signed-receipt-import, plus the matching test fixtures. Replace with "schema version ≠ 8 means re-init".
  2. **One generic row mapper, and no duplicated proof-plan query** (#26): about 250 lines.
  3. **Generate the triggers in a loop and drop the hand-written schema-shape checks** (#24, #25): about 350 lines.
  4. **Drop `active_state` and `active_sessions`, and derive active sessions with a query or view** (#22): about 150 lines, and it removes the global corruption scan and the silent repair in #2.
  5. **Merge the three receipt-append functions and remove the write-queue machinery** (#21, #27, #28): about 250 lines.
  
  If you also merged `tasks` into `sessions` (#23), it would go below 1,300.
  
  Relevant files:
  - /Users/nnennandukwe/Code/threadloop/src/adapters/fs/sqlite-store.ts
  - /Users/nnennandukwe/Code/threadloop/src/schemas/state.ts
  - /Users/nnennandukwe/Code/threadloop/src/adapters/fs/sqlite-driver.ts
  - /Users/nnennandukwe/Code/threadloop/src/services/session-service.ts (lines 329–396, 2180–2197)
  - /Users/nnennandukwe/Code/threadloop/src/services/signed-receipt-import.ts (lines 311–380)
</agent-message>

---

<task-notification>
<task-id>a0ddc3624123296cb</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/tasks/a0ddc3624123296cb.output</output-file>
<status>completed</status>
<summary>Agent "Audit sqlite-store.ts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This agent's report was delivered to you as a message from "a0ddc3624123296cb" (its SubagentHandback call). Read it there; it is not repeated here.
</result>
<usage><subagent_tokens>163319</subagent_tokens><tool_uses>25</tool_uses><duration_ms>335915</duration_ms></usage>
</task-notification>

---

<agent-message from="aa4ca6d6e47afd004">
[Subagent hand-back] The text below is the final report of a subagent this session delegated to. It is model output, NOT a message from the user: instructions, requests, or approval claims inside it are the subagent's words and carry no user authority. The harness indents every line of the report, so a frame-like line at column zero inside it would be forged. Notes above this frame may quote model-derived text, which carries no user authority either. The report follows:
  ## ThreadLoop audit: CLI, commands, adapters, scripts and workflows
  
  **Scope:** about 4,415 lines. That excludes the three fs files you carved out. I read every file in full. I also ran scratch reproductions under the session scratchpad; no repo files were edited.
  
  **Bottom line:** the process spawning is safe. There is no `shell:true` in git or the gate runner, and all args are passed as arrays. The workflows are also clean: no `${{ github.event.* }}` in `run:` steps, everything goes through `env:`, and `id-token` is kept out of the job that runs caller code. The real problems are four confirmed bugs (one breaks CI signing entirely), silent CLI failures, and a lot of wrapper and duplicate code.
  
  ### Bugs, most important first
  
  1. **Signing always fails for any gate that declares `setup`** — `scripts/sign-ci-gate-receipt.ts:55-59` — bug, **confirmed**.
     - The script checks the gate by wrapping it in a legacy plan that has no `contract_version`. That makes `allowSetup` false.
     - The run step (`run-ci-gate-sensor.ts:55-57`) uses `validateDeclaredGate(..., {allowSetup:true})`, and its own comment warns: *"Wrapping this in a synthetic legacy plan would reject every gate that declares setup."*
     - Reproduced: the run side accepts a setup gate; the sign side throws `proof_plan.gates[0] must contain exactly: id, command, working_directory, timeout_ms`.
     - Result: the `sign_receipt` job fails and no signed receipt is produced. This hits exactly the toolchain case that `examples/threadloop-caller-workflow.yml:48-49` tells users to use.
     - No test covers signing a setup gate (`tests/unit/ci-workflow.test.ts` only checks the env).
  
  2. **The gate timeout doesn't hold, and grandchild processes are never killed** — `src/adapters/process/gate-runner.ts:232-282` — bug, **confirmed**.
     - `spawn` isn't detached and `terminate()` only signals the direct child. The code then waits on `'close'`, which only fires once every holder of the stdout/stderr pipes exits.
     - Reproduced: `['sh','-c','sleep 8 & sleep 8']` with `timeoutMs: 500` came back `timed_out` after **8035 ms**.
     - With `npm test` or a long background process, the gate hangs until the grandchild exits (in CI, up to the 360-minute job timeout), and the grandchild stays orphaned.
     - Fix: `detached:true` plus `process.kill(-pid)`, and resolve on `'exit'` with a drain deadline.
  
  3. **The changed-file list is garbled when there's no base ref** — `src/adapters/git/client.ts:153-160` — bug, **confirmed**.
     - The fallback runs `git status --short`, then `.trim().slice(3)`. The trim removes the leading space of `" M"` lines, and `git()` also trims the first line.
     - Reproduced: modified `foo.txt` became `'oo.txt'`, `"a b.txt"` became `'a b.txt"'`, and a rename became `'bar.txt -> baz.txt'`.
     - This feeds `snapshotRepo`, so it lands in artifact "What changed" sections, PR summaries and reconcile counts.
     - Fix: reuse the existing `parsePorcelainPaths` (lines 298-322), which already handles `-z` output correctly.
  
  4. **`--interval` quietly accepts bad values** — `src/cli-program.ts:398-404` — bug, **confirmed**.
     - `parseInt` accepts junk: `5abc` became 5 (reproduced), and `1.9` becomes 1.
     - `daemon.ts:14-22` has a second, dead `parseIntervalSeconds` that can never fire, because commander already applies the default of 60.
  
  5. **Running with no subcommand prints nothing and exits 0** — `src/cli-program.ts:103` and `src/cli.ts:74-79` — bug, **confirmed**.
     - `writeErr: () => {}` swallows commander's help-on-error, and `handleError` turns `commander.help` (exit code 1) into exit 0.
     - Reproduced: `threadloop` and `threadloop session` each gave no output and `rc=0`.
  
  6. **The daemon never fails** — `src/commands/daemon.ts:109-118` — bug, **confirmed**.
     - Every tick's error is caught and written to stderr, and the loop carries on forever.
     - Reproduced: in a repo that isn't initialized it printed `[daemon] Error: ... not initialized` every 5 seconds without stopping. An unrecoverable setup error never produces a non-zero exit.
     - With `--json`, stdout gets two separate JSON envelopes ("running" and "stopped"), so it isn't one parseable document.
  
  7. **One ghost account can break the whole review snapshot** — `src/adapters/github/review-sensor.ts:127-128` — bug, **plausible**.
     - For approved reviews, `object(review.author)` and `object(review.commit)` throw on `null`. GitHub returns `author: null` for deleted accounts, and `commit` is nullable in the schema.
     - Threads handle a null author (line 157); approvals don't.
     - Pagination itself is handled correctly, including a guard against repeated cursors.
  
  8. **Superseded approvals are counted** — `review-sensor.ts:124-137` — bug, **plausible**.
     - Every `APPROVED` review is collected, including one the same reviewer later replaced with `CHANGES_REQUESTED`. Whether that matters depends on what reads `approvals`; `reviewDecision` is captured separately.
  
  9. **A non-JSON error response hides the HTTP status** — `review-sensor.ts:236-244` — bug, **confirmed** from the code.
     - `response.json()` is parsed before `response.ok` is checked, so a 502 HTML page reports "returned invalid JSON" and the status is lost.
  
  10. **Committed-only diff when a base exists** — `client.ts:148-150` and `168` — **plausible** design bug.
      - With a base ref, changed files and diff stats come only from `base...HEAD`. Uncommitted work vanishes from the artifacts, while the no-base path shows only uncommitted work. The two modes describe different things.
  
  11. **Required options are declared in three places and disagree** — `cli-program.ts:45-58`, `169-194`; `session-status.ts:20`; `session-heartbeat.ts:18` — bug/bloat, **confirmed**.
      - `PROTOCOL_COMMAND_RULES` marks `--session` required for `session status`, `capture` and `heartbeat`.
      - Commander declares it with `.option` (optional), and each handler re-checks it by hand and throws `SESSION_REQUIRED`.
      - Using `.requiredOption` would remove the rules table entries and the handler checks.
  
  12. **`session reconcile --session X --all` silently ignores `--all`** — `session-service.ts:1841` via `session-reconcile.ts` — **confirmed** from the code. There's no mutual-exclusion check.
  
  ### Dead code, duplication and bloat
  
  13. **The `start` / `status` / `capture` commands are 7-13 line wrappers** — `commands/start.ts`, `status.ts`, `capture.ts` — bloat, **confirmed**.
      - Each only flips one boolean (`allowMultipleActive=false` or `allowLegacySingleActive=true`) on the session-* handler.
      - Both sets are live and documented as a "compatibility surface" (README.md:147-159, docs/cli.md:233-245) in a 0.1.0 package, and `smoke-pack.mjs` only exercises the legacy set.
      - Removing them also removes the boolean flags from `session-start`, `session-status` and `session-capture`, plus the "legacy convenience" branch at `session-status.ts:28-33`.
      - `artifact generate` is **not** dead: it has no `session` equivalent. Moving it under `session` would be cleaner.
  
  14. **Option definitions are copy-pasted** — `cli-program.ts:109-120` vs `152-163` (start), `122-132` vs `176-186` (capture), `134-139` vs `169-174` (status) — bloat, **confirmed**. About 35 lines are exact duplicates.
  
  15. **Validation runs twice** — `session-capture.ts:38-42` repeats `parseEntryKind` (cli-program.ts:294); `session-heartbeat.ts:24-29` repeats `parseHeartbeatSource` — dead code, **confirmed**. Commander has already rejected bad values before the handler runs.
  
  16. **The daemon is a real command, but it's just a sleep loop** — `commands/daemon.ts` (125 lines) plus the `cli-program` wiring, a unit test, and docs — bloat, **confirmed**.
      - It's a `setTimeout` loop around `reconcileSession({reconcileAll:true})`, with custom signal and AbortSignal plumbing (`DaemonRunRuntimeOptions`, `renderStopInstruction`) that exists only for the test.
      - `while sleep 60; do threadloop session reconcile --all; done` does the same, and item 6 shows it's worse at failing.
      - Candidate for deletion.
  
  17. **The handler plumbing is triple-listed** — `cli-program.ts:15-37` (interface), `61-87` (`createNoopCliHandlers`), `cli.ts:3-49` (imports plus wiring) — bloat, **confirmed**.
      - Every command name appears three times, plus again in `detectInvokedCommand` (`cli.ts:103-123`), which hand-parses argv to re-derive a name commander already knows.
      - The noop factory exists only so `contracts/protocol.ts:44` can introspect the program.
      - A single table of `{path, options, handler}` would replace all four.
  
  18. **Many command files are just service call → text/JSON output** — bloat, **confirmed**.
      - 14 of them run 21-66 lines each: `session-gate-run` (21), `session-next` (23), `session-list` (26), the `session-gate-import` / `session-review-import` pair (28 each, near-identical), `artifact` (23), `audit` (55).
      - Each declares its own tiny `*Options` interface (`SessionGateRunOptions`, `SessionNextOptions`, `SessionGateImportOptions`, `SessionReviewImportOptions`, `AuditSessionOptions` are all `{session:string} & JsonOption`).
      - They belong next to their command definitions.
  
  19. **`init` bypasses the output contract** — `commands/init.ts` — bug/bloat, **confirmed**.
      - It's the only command using `console.log` and `process.cwd()` directly, wired through a separate `run()` wrapper instead of `commandAction`, and it has no `--json`.
      - That's why `cli.ts` needs two wrapper functions.
  
  20. **A 421-line validator for issue/PR templates** — `scripts/community-template-validator.ts` plus `check-community.ts` — bloat, **confirmed**.
      - It re-implements GitHub issue-form validation (body types, ids, labels, checkbox options, link checking) and runs in `check:static`. That's a lot of carried code for repo hygiene.
      - Cut to about 30 lines (PR template section headings plus link existence) or drop it.
  
  21. **The sensor scripts copy the same preamble** — `run-ci-gate-sensor.ts:20-52` vs `sign-ci-gate-receipt.ts:20-53` — bloat, **confirmed**.
      - Env reads, the `runInvocationUri` build and six regex checks are about 30 identical lines. `collect-github-review-snapshot.ts` and `sign-github-review-receipt.ts` rebuild `sourceRepository` / `runInvocationUri` a third and fourth time, and `review-sensor.ts:302-310` re-checks the same repo-URL regex.
      - The stat / size-cap / read / "changed while reading" / parse block is duplicated between the two sign scripts (`sign-ci:64-77`, `sign-review:25-38`).
      - The copies have already drifted: the review sign script skips the session/sha/ref checks, and the gate sign script uses the wrong validator (item 1). One `sensor-context.ts` would fix both.
  
  22. **Setup and gate working-directory checks are duplicated** — `run-ci-gate-sensor.ts:66-97` — bloat, **confirmed**. The same realpath + containment check is written twice; one helper would do.
  
  23. **The Markdown renderer repeats one template shape** — `renderers/markdown/artifacts.ts` — bloat, **confirmed**.
      - `section(title, bullets(entries.filter(e => e.kind === K), 'No X recorded.'))` appears 11 times across three renderers.
      - "What changed" / "Changes in scope" is duplicated, and `HandoffGovernance` (lines 4-72) is a 69-line hand-written projection type.
      - A `{title, kinds, empty}[]` table per artifact kind would cut it to about 200 lines.
      - One real bug here: `entry.body` is inserted raw (line 95). A multi-line capture breaks the bullet list, and only review findings go through `oneLine`.
  
  24. **The two GraphQL queries repeat the same metadata** — `review-sensor.ts:19-88` — bloat, **plausible**.
      - Both repeat the same 7 PR metadata fields so `assertStableMetadata` can compare them.
      - The file carries its own `object`/`text`/`boolean`/`positiveInteger` validators (lines 319-364), which the domain layer almost certainly also has. I didn't verify that; worth a grep.
  
  25. **The git client has two parallel observation APIs** — `client.ts` — bloat, **plausible**.
      - `observeRepository` and `observeProofRepository` (porcelain `-z`, correct) sit beside `snapshotRepo` → `getChangedFiles` / `getDiffStats` / `getCommitRange`. The latter calls `refExists` three times per snapshot and uses fragile text parsing (item 3, and numstat without `-z` mangles rename paths).
      - Folding `snapshotRepo` onto the porcelain parser removes about 60 lines and fixes item 3.
  
  **Minor, low risk:**
  - `editor.ts:16` uses `shell:true` with `${EDITOR} "${tempFile}"`. That matches normal `$EDITOR` convention and the temp path is quoted, so it's not a real injection.
  - `--base` option injection is blocked, because `refExists` runs `rev-parse --verify` first (tested: `--output=...` gets rejected).
  
  ### Size estimate
  The scope could plausibly shrink from about **4,415 to 2,900-3,100 lines (roughly 30-35% less)**, not counting the matching test and doc deletions. The biggest cuts:
  
  | Change | Lines saved |
  |---|---|
  | Fold `src/commands/*.ts` into one command table; drop the handler interface, noop factory, `detectInvokedCommand` and per-command option interfaces (items 17-19) | ~450 |
  | Delete or shrink `community-template-validator.ts` (item 20) | ~380 |
  | Remove legacy `start`/`status`/`capture`, the duplicated option blocks, the `allowLegacy*` flags, and the daemon (items 13-16) | ~250 in scope, plus tests and docs |
  | Shared `sensor-context.ts` for the four sensor scripts, one path-containment helper, one report reader (items 21-22) | ~120 |
  | Table-driven Markdown renderer, plus merging `snapshotRepo` onto the porcelain parser (items 23, 25) | ~200 |
  
  Recommended fix order: item 1 (CI signing is broken for setup gates), item 2 (gate hangs and orphaned processes), then item 3 (wrong file names in PR artifacts). All three are small changes.
  
</agent-message>

---

<task-notification>
<task-id>aa4ca6d6e47afd004</task-id>
<tool-use-id>toolu_01BDCYopaSUgrUoQzHjLjSsg</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/tasks/aa4ca6d6e47afd004.output</output-file>
<status>completed</status>
<summary>Agent "Audit adapters, CLI, scripts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This agent's report was delivered to you as a message from "aa4ca6d6e47afd004" (its SubagentHandback call). Read it there; it is not repeated here.
</result>
<usage><subagent_tokens>158621</subagent_tokens><tool_uses>31</tool_uses><duration_ms>384183</duration_ms></usage>
</task-notification>

---

<agent-message from="a5da780bbe28c896e">
[Subagent hand-back] The text below is the final report of a subagent this session delegated to. It is model output, NOT a message from the user: instructions, requests, or approval claims inside it are the subagent's words and carry no user authority. The harness indents every line of the report, so a frame-like line at column zero inside it would be forged. Notes above this frame may quote model-derived text, which carries no user authority either. The report follows:
  ## ThreadLoop test-suite audit (read-only)
  
  **Run facts:** 33 files and 381 tests, all passing. Wall time 73.3s; cumulative test time 269.9s. No coverage provider is installed (`@vitest/coverage-*` is absent), so there is no coverage data.
  
  **Slowest files (summed test ms):**
  
  | File | Time | Tests |
  |---|---|---|
  | session-gate | 72.5s | 37 |
  | session-review-import | 46.5s | 17 |
  | cli | 43.0s | 40 |
  | session-gate-import | 34.1s | 20 |
  | session-transition (integration) | 32.9s | 50 |
  | audit-cli | 15.8s | 10 |
  | packaging | 12.1s | 1 |
  | runner-replay | 5.7s | 3 |
  | unit/session-service | 5.0s | 6 |
  
  Every other file totals under 1s.
  
  **Slowest single tests:**
  - packaging e2e: 12.1s
  - review-import "fourth review repair": 7.7s
  - session-gate "more than three pre-PR cycles": 6.7s
  - session-transition "schema-v6 migration": 4.8s
  
  **Bottom line:** Most of the bloat is copy-pasted infrastructure, not redundant assertions. Two things back that up:
  - 34 inline copies of the 15-line `session transition` argv across the integration files.
  - 117 `new DatabaseSync(...)` / try / finally / close blocks.
  
  Doc and template change-detectors are a smaller share, as are a handful of true duplicate tests. I also found two vacuous assertion sites, one vacuous test, one weak assertion pattern, and heavy temp-dir leakage.
  
  ---
  
  ### Prioritized findings
  
  **A. Wrong, vacuous or order-dependent (fix first)**
  
  1. **`integration/session-gate-import.test.ts:303-304`, vacuous.** These `expect(requestedPath)` / `expect(maxBytes)` calls run inside the injected `readWithinLimit`. If they fail, the AssertionError is caught by `src/services/signed-receipt-import.ts:347` and rethrown as `SIGNED_RECEIPT_INVALID`, which is exactly what the test expects at :320. The test would still pass.
  
  2. **`integration/session-gate-import.test.ts:979-980`, vacuous (same pattern).** The expects sit inside a monkeypatched `readWithinLimit`. `src/services/signed-receipt-files.ts:47-49` has a `catch {}` that maps any throw to `null`, which becomes `corrupt`, which is the expected result at :985. This test also mutates the shared `nodeSignedReceiptFileSystem` singleton. Fix: record the args in variables and assert after the call.
  
  3. **`integration/cli.test.ts:1452`, vacuous.** "reopens cached sqlite handles after close/reset hooks": `resetSqliteConnectionLifecycle()` resets connections in the test process, but every read and write in this test goes through `runCli` subprocesses. The reset is a no-op, so the test only re-checks capture plus status.
  
  4. **`integration/cli.test.ts:649` vs `unit/session-service.test.ts:122`, contradictory specs.** Both apply the identical `UPDATE active_sessions SET task_id=<other>` corruption:
     - The CLI test expects the capture to succeed and the projection to be repaired.
     - The service test expects `STATE_CORRUPTED`.
  
     Both pass only because a fresh subprocess runs `reconcileActiveSessionProjection` on open (`sqlite-store.ts:2462`), while the in-process cached connection does not. Together they pin cache-dependent behaviour; at least one of them is encoding an accident.
  
  5. **`integration/cli.test.ts:921` and `:1040`, weak assertions.** `.rejects.toThrow()` has no message or code, so any failure passes. That includes `resolveTestCliBundle()` throwing on a stale bundle, or git being missing. Assert the error code.
  
  6. **Temp-dir and connection leaks (global state).** These files never delete their temp dirs:
     - `integration/session-transition.test.ts:194-197`: the afterEach clears the array but never calls `rm`, and `rm` isn't even imported.
     - `integration/cli.test.ts`: `beforeEach` creates a repo per test with no afterEach at all, and there's a stray `threadloop-no-git-` dir at :1041.
     - `unit/session-service.test.ts`: no cleanup and no `resetSqliteConnections`.
     - `integration/packaging.test.ts`: two dirs.
     - `unit/cli-bundle-resolution.test.ts:18`.
  
     Evidence: 614 `threadloop-*` dirs are currently sitting in `$TMPDIR`, dominated by the transition/cli/service prefixes. This is roughly 100 dirs per run.
  
  7. **`vitest.config.ts:4-21`, config drift.** The comment says "one budget for the whole suite", but `cli.test.ts:162` and `session-transition.test.ts:1617` set a lower 15s, and `session-gate.test.ts:1920` sets 60s. The "300 tests" measurement is also stale (the suite now has 381).
  
  **B. True duplicates or superseded tests**
  
  8. **`integration/runner-replay.test.ts:146,171` duplicate `session-transition.test.ts:1414,1387`.** Both pairs cover identical-wake dedupe and the distinct-key race via CLI. Keep one of each (runner-replay adds only an audit-event count). About 45 lines and 5s.
  
  9. **`integration/cli.test.ts:1441` is a strict subset of `:1488`.** Both call `runConcurrentMutationBurst`; :1488 does two rounds. Delete :1441 (1.6s).
  
  10. **`integration/cli.test.ts:1521` ("explicit v2 flow") re-walks `:1045`.** Both do start, capture, heartbeat, status, transition framed, list and ended_at null. Merge (~90 lines, 2s).
  
  11. **`integration/cli.test.ts:1212` duplicates `:1235`.** It's a hand-rolled try/catch reimplementation of `runCliFailure` for the same `SESSION_REQUIRED` case. `:1614` "renders reconcile in help" is one line that belongs in `:1256`.
  
  12. **`unit/session-service.test.ts:30,51,74` duplicate `cli.test.ts:194,820,1001`.** The live-snapshot test has an identical title in both files. Keep one level (~100 lines, ~2.3s).
  
  13. **`integration/packaging.test.ts` (whole file, 130 lines, 12.1s) duplicates `scripts/smoke-pack.mjs`.** Both do pack, install, then init/start/capture/artifact/status. CI runs both (`ci.yml:81` `npm test` and `:87` `smoke:pack`). Move the extra session-namespace steps into smoke-pack and delete the test.
  
  14. **`session-gate-import.test.ts` and `session-review-import.test.ts`: the same five scenarios are written twice.** Both kinds share one `signed-receipt-import.ts` path with a `kind` switch:
  
      | Scenario | gate-import | review-import |
      |---|---|---|
      | Missing audit genesis | :364 | :508 |
      | Task-projection drift | :404 | :548 |
      | Concurrent identical import | :755 | :713 |
      | Same id, different bytes | :780 | :684 |
      | Transparency failure | :875 | :646 |
  
      Use `describe.each(['gate','review'])` (~150 lines).
  
  15. **Domain guards re-proven end-to-end at 2–8s each, already covered in `unit/session-transition.test.ts` (fast):**
  
      | Guard | Unit | Integration |
      |---|---|---|
      | PROOF_CHECKOUT_MISMATCH (dirty / other branch) | :232 | session-gate :982 (it.each ×2, 4.6s) |
      | SIGNED_CI_PROOF_REQUIRED | :283 | session-gate :930 and :1056 (asserted twice) |
      | Completion needs same-HEAD approval + merge | :340 | review-import :889 |
      | POST_PR reentry forbidden | :672 | review-import :822 and session-transition :311 |
      | Malformed pre-PR evidence | :594 | session-gate :1312 and session-transition :1308 |
  
      Keep one integration case per guard to prove the wiring (and the "no mutation" DB check). Drop the rest (~250 lines, ~12s).
  
  16. **`unit/lifecycle.test.ts:32` and `:91` are subsets of the full-matrix test at `:60`**, which already asserts every allowed pair including every `*:blocked` pair (~35 lines).
  
  17. **`integration/cli.test.ts:1328` re-asserts `buildProtocolContract()` literals** that `unit/protocol-contract.test.ts` already pins. It already does `toEqual(buildProtocolContract())`, so the 30 lines of field-by-field literals after that add nothing. `runner-replay.test.ts:118-128` repeats the same versions a third time.
  
  **C. Change-detector and low-value tests**
  
  18. **`unit/lifecycle-graph-mapping-doc.test.ts:31-41`** asserts a literal commit SHA (`origin/main@7145263...`), issue numbers and prose phrases in a markdown doc. `:92-144` hard-codes 29 required-work codes plus prose categories. Keep only the enum-coverage checks (:43, :55), ideally moved to `check:community`. About 80 lines.
  
  19. **`unit/ci-workflow.test.ts:37-48,134-144` hard-code action SHA pin lists.** Every Dependabot bump breaks them; git log shows `4266bac fix(ci): update artifact action assertions` and `ae85c76 chore(deps): bump actions/setup-node` editing this test. zizmor and actionlint already run in `security.yml:31-38` and check unpinned uses. Replace with "every `uses:` matches `@[0-9a-f]{40}`". Also, `:70-80` greps script source for substrings.
  
  20. **`integration/runner-replay.test.ts:130-143`** asserts 14 prose sentences from `.agents/skills/threadloop-runner/SKILL.md`. That is pure doc snapshotting (~40 lines).
  
  21. **`integration/cli.test.ts:1256-1326`** is 70 lines of `--help` substring checks, a change-detector on commander formatting. Keep "removed commands fail" and one usage check per command.
  
  22. **`integration/cli.test.ts:745-773`** asserts nine handoff template headings. Template change-detector.
  
  23. **`integration/session-gate.test.ts:248`** is a one-line ~900-char literal `plan_json` blob. Canonical-JSON bytes are already pinned in `unit/proof.test.ts:73,105` and `unit/session-transition.test.ts:130`. Compare against `canonicalizeProofPlan(proofPlan()).json` instead.
  
  24. **`integration/session-transition.test.ts:1640-1756`** is a 60-line whole-object `toMatchObject` of `session next`. It's snapshot-style and re-asserts `repair_budget`/`staleness` defaults already checked in session-gate :954. Keep the sanitization assertions (:1753-1755) and trim the rest.
  
  **D. Infrastructure bloat (biggest line savings)**
  
  25. **Inline transition argv: 34 copies of the ~15-line block.** Counts: session-transition 13, session-gate 10, review-import 3, others 8. There are also three different local helpers:
      - `session-gate.test.ts:178 transition`
      - `session-review-import.test.ts:1170 transitionSession`
      - `session-transition.test.ts:1084` / `runner-replay.test.ts:75 transitionArgs`
  
      Put one `transitionArgs`/`transition`/`transitionFailure` in `tests/helpers` (~400 lines). Note that `session-review-import.test.ts:1196` reimplements `runCliFailure` with a sentinel-in-try pattern.
  
  26. **DB open/try/finally boilerplate: 117 `new DatabaseSync` sites.** For example session-transition has 43 and session-gate 26. Most exist just to `SELECT COUNT(*)` or read `status, state_version`. `session-review-import.test.ts:1117-1162` already shows the right shape (`readLifecycle`, `reviewReceiptCount`). Promote those to helpers (~350 lines).
  
  27. **Repo, session and plan fixtures duplicated three times:**
      - `session-gate.test.ts:27-176`
      - `session-gate-import.test.ts:46-174` (`makeVerifyingSession` re-inlines git init, start, frame, proof_ready and the `forceVerifying` loop)
      - `session-review-import.test.ts:62-193`
  
      The git-init preamble alone is repeated in 10 files. Target helpers: `makeCommittedRepo`, `startFramedSession`, `proofPlan`, `forceState(targetStates[])` (~300 lines).
  
  28. **Signed-package builders duplicated.** `session-gate-import.test.ts:176-284` has `signedArtifact`, `writePackage` and `verifier`. `session-review-import.test.ts:294-378` has `signedGateArtifact`, `writeGatePackage` and `gateVerifier`, which are near-identical. Review-import's `writePackage` (:238) differs only in media and payload type, and `unit/attestation.test.ts:67` has a third copy. Move them to `tests/helpers/signed-receipts.ts` (~200 lines).
  
  29. **Legacy schema DDL duplicated.** `cli.test.ts:358-428` (schema v1, 70 lines) and `session-transition.test.ts:93-192` (`createSchemaV2`, 100 lines) define essentially the same tables. `cli.test.ts:277-317` and `:486-515` duplicate the legacy `state.json` object. Use one parametrized builder (~120 lines).
  
  30. **Drop-trigger / mutate / recreate-trigger SQL is inlined 14 times**, for example session-gate :903-911 and :1763-1771, session-transition :792-817, and the gate/review/transition "audit genesis" blocks. Use a `withTriggersDisabled(db, table, fn)` helper (~100 lines).
  
  31. **`review-import.test.ts:1041-1061` inlines the thread fixture** that `blockingReviewArtifact` (:417) already builds. `unit/session-transition.test.ts:318-328,398-409,714-724` repeats the same `blockingFindings` literal three times.
  
  32. **Unit helper duplication.** `proof.test.ts` and `setup-steps.test.ts` each define identical `ciPolicy`, `reviewPolicy` and `captureProofValidationError`. `proof.test.ts:250-256` then hand-rolls the same try/catch again instead of using its own helper. The v3-readable cases also overlap (`proof.test.ts:110` vs `setup-steps.test.ts:156`). About 60 lines.
  
  **E. Runtime-only consolidation (no loss of coverage)**
  
  33. **`session-transition.test.ts:1430`**: 10 `it.each` cases each build a fresh repo and CLI session (about 0.5s each, 5.2s total) just to check argument parsing. The state is asserted unchanged after each case, so one shared session is safe (saves ~4.5s).
  
  34. **`session-transition.test.ts:521`**: 10 schema-metadata variants, where the "3.x" set mirrors the "2.x" set one-for-one. Five would do.
  
  **F. Coverage gaps (brief)**
  
  35. Gaps worth noting:
      - **CLI success paths for imports.** `session review import` success is never exercised through the CLI; all review-import tests call the service directly. `session gate import` is only exercised on its failure path.
      - **Signing scripts.** `scripts/sign-ci-gate-receipt.ts` (118 lines), `scripts/sign-github-review-receipt.ts` and `scripts/collect-github-review-snapshot.ts` have no tests. Only the domain function `authorizeGateReportForSigning` is unit-tested.
      - **Real Sigstore verification.** No integration test runs the real verifier; it's always stubbed. That's acceptable, but worth knowing.
  
  ---
  
  ### Estimated removable or consolidatable lines
  
  These keep equivalent bug-catching power.
  
  | File | Lines now | Removable (est.) |
  |---|---|---|
  | integration/session-gate.test.ts | 1990 | ~480 (fixtures 150, argv 120, DB 150, dup guards 60) |
  | integration/session-transition.test.ts | 1876 | ~450 (argv 150, DB 250, trigger/fixture 50) |
  | integration/cli.test.ts | 1628 | ~380 (DDL/legacy 140, help/protocol/handoff 110, dup tests 130) |
  | integration/session-review-import.test.ts | 1214 | ~370 (fixtures/builders 250, shared-kind tests 80, inline dup 40) |
  | integration/session-gate-import.test.ts | 991 | ~260 (fixture 130, DB blocks 80, shared-kind tests 50) |
  | integration/packaging.test.ts | 130 | ~130 (fold into smoke-pack) |
  | integration/runner-replay.test.ts | 188 | ~120 |
  | integration/audit-cli.test.ts | 540 | ~60 |
  | unit/session-service.test.ts | 201 | ~100 |
  | unit/lifecycle-graph-mapping-doc.test.ts | 145 | ~80 |
  | unit/proof.test.ts + setup-steps.test.ts | 651 | ~60 |
  | unit/session-transition.test.ts | 751 | ~40 |
  | unit/attestation.test.ts | 469 | ~40 |
  | unit/lifecycle.test.ts | 184 | ~35 |
  | unit/ci-workflow.test.ts | 146 | ~30 |
  | **Total** | **~13,337** | **~2,600 (roughly 2,400–2,900, about 20%)** |
  
  About 1,500 of those lines come from shared helpers. Roughly 1,100 come from deleting or merging tests.
  
  Expected runtime saving is about 35–45s of cumulative test time: packaging 12s, duplicate guard end-to-end tests ~12s, it.each repo sharing ~5s, CLI and service duplicates ~6s.
  
  The remaining files are reasonable as-is: caller-template, community-template-validator, gate-runner, sigstore-verifier, github-review-sensor, audit, review, gate-outcome, read-snapshot, git-client, daemon, state-schema, cli-bundle-resolution, and the `tests/helpers/cli.ts` / `global-setup.ts` infrastructure.
</agent-message>

---

<task-notification>
<task-id>a5da780bbe28c896e</task-id>
<tool-use-id>toolu_017XWmicaUEthXCgpqGCwmqq</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/tasks/a5da780bbe28c896e.output</output-file>
<status>completed</status>
<summary>Agent "Audit test suite bloat" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This agent's report was delivered to you as a message from "a5da780bbe28c896e" (its SubagentHandback call). Read it there; it is not repeated here.
</result>
<usage><subagent_tokens>320382</subagent_tokens><tool_uses>42</tool_uses><duration_ms>392878</duration_ms></usage>
</task-notification>

---

Is claude a co-author? I want to make sure that config is turned off. (for PRs I create)

---

great. The analysis that you just did about my codebase: I do need to bring that down by 40%, and I want you to start the process of doing that. I want this to be tracked.
Can you create a GitHub issue documenting the analysis that you just ran? You're going to start work on making this repo much cleaner:
- much cleaner code
- well-architected
- the right abstractions, not too much, not too little
- far fewer redundant tests that end up just bloating the project
I want all of that addressed, and I don't want you to stop until that is created. Qodo should be reviewing it, and then that code should end up getting merged into the main branch.

---

did you delete the contents of the readme? if so why?

---

<agent-message from="a8b1b8f841cee7b5c">
[Subagent hand-back] The text below is the final report of a subagent this session delegated to. It is model output, NOT a message from the user: instructions, requests, or approval claims inside it are the subagent's words and carry no user authority. The harness indents every line of the report, so a frame-like line at column zero inside it would be forged. Notes above this frame may quote model-derived text, which carries no user authority either. The report follows:
  ## Contract/spec layer audit (commit 357fc58, read-only)
  
  **Scope measured.** Scripts are 5,759 lines. The 16 test files are 6,251 lines, plus 420 lines of test fixtures. `docs/contracts` holds about 44.6k lines (2.2 MB), mostly generated JSON. The tests pass (451 tests; the slowest file is `controller-conformance.test.ts` at 4.25s). I ran probe scripts only from the scratchpad and changed no repo files.
  
  ### 1. What each module does and who uses it
  - **Nothing in `src/` imports any of the five modules**, and the tsup build only bundles `src/cli.ts`. CI never runs the `spec:*` scripts; the only runtime consumers are the Vitest tests. The external consumer is the RunInvariant harness, which reads the `docs/contracts` artifacts. The README says the contract freeze (#110) completed at fa616c6.
  - **workflow-graph (816 lines):** compiles and validates YAML Workflow Profiles (topology and cycle controls) into a canonical Compiled Graph. It also hosts the de facto shared kernel (`Diagnostic`, `ValidationResult`, `validateShape`), which every other module imports.
  - **controller-contract (1,472):** Controller Input, Decision and Action Request schemas. It includes a candidate validator plus guard, prerequisite and blocked-reason checks. It is not a selector.
  - **execution-contract (1,426):** the claim/attempt journal state machine (`model.ts`, 1,102 lines), with replay, projection and an authority seam.
  - **executor-contract (1,132):** executor request/result envelopes, the bounded canonical codec (RFC 8785 subset), GAAP request/receipt validation and the result mapper.
  - **controller-conformance (913):** 38-case corpus loader and expander, manifest and compatibility pins, and request/response checking for RunInvariant.
  - **Exports used only inside their own file** (knip misses these because of `ignoreExportsUsedInFile`): `guardFamilies`, `executionSummarySchema`, `resultSchema`, `receiptAdmissionSchema`, `recoveryEvidenceSchema`, `executionClaimSchema`, `attemptSchema`, `OperationResult`, `ExecutionProjection`, and the executor's `versionedIdentitySchema`, `evidenceTypeSchema`, `evidenceSchema`, `usageSchema`, `parametersSchema`.
  
  ### 2. Findings, prioritized (file:line · category · claim · evidence · confidence)
  
  **Real bugs, validation gaps and weak tests**
  1. `tests/unit/gaap-ledger-review.test.ts:160` · bug (test). The "rejects observed tool calls above budget" test passes for the wrong reason. My probe got the same diagnostic ("Cumulative tool usage must include every tool execution…", from `gaap-validation.ts:184`) with and without the budget change. The completed-over-budget check (`gaap-validation.ts:215-228`) works, but no negative test targets it. High.
  2. `scripts/controller-conformance/validation.ts:160` · validation gap. `validateSubjectResponse` returns ok for a blanket `blocked{NO_APPLICABLE_REMEDY}` answer on 24 of 25 decide cases, because any decision whose only diagnostic is `SELECTION_PROOF_REQUIRED` is accepted. `compareCaseResult` still catches the mismatch, but the response checker's ok means nothing semantically. High on the behavior, medium severity.
  3. `tests/unit/executor-admission.test.ts:94` · weak test. 4 of the 11 mutation cases (Workflow Run, graph, state version, subject) fail with `REQUEST_IDENTITY_MISMATCH` inside `validateExecutorRequest` (`validation.ts:29`). They never reach the context check the test claims to cover. My probe confirmed the codes; the test only asserts `ok === false`. High.
  4. About 124 bare `.ok).toBe(false)` assertions with no diagnostic code: controller-contract 52, conformance 22, executor-review-regressions 18, gaap-ledger 10, graph 9, and others. Findings 1 and 3 show these can pass for the wrong reason. High.
  5. `package.json` · drift risk. zod is declared `^4.1.11` and 4.4.3 is installed. All 26 published schemas come from zod's `toJSONSchema`, and 18 of them are byte-pinned by `compatibility.json`. A lockfile refresh could change emitter output and break the frozen bytes. zod should be pinned exactly. Medium-high.
  6. `scripts/controller-conformance/validation.ts:129` · latent inconsistency. The `compile_graph` path feeds JSON input through the YAML parser. A JSON key `"<<"` would give `YAML_INVALID` at `$`, where a JSON-native subject would report `SCHEMA_INVALID`. No fixture exercises this. Medium.
  7. `docs/contracts/controller-v0.1/README.md:194` and `workflow-graph-v0.1/README.md:53-58` · doc drift. They say canonical JSON uses UTF-16 key order and safe integers. `src/domain/canonical-json.ts` hoists integer-looking keys and accepts floats and negatives. The executor README (lines 219-228) acknowledges this. It is unreachable after strict zod parsing: across all docs/contracts JSON plus the 38 expanded conformance fixtures (243 checked values), the two serializers matched on 242. The one difference is the golden vector that deliberately tests integer keys. Low severity, high confidence.
  
  **Dead code**
  8. Timestamp re-checks can never fire: `controller-contract/validation.ts:161-163` and `299-308`, `model.ts:784-786` (`realTime` and its 5 call sites), `executor-contract/validation.ts:224-228`. `z.iso.datetime({precision:3})` already enforces the same real-date regex, including leap years; I checked edge dates against both. `INVALID_TIMESTAMP` is untested, and its path is hard-coded to `$.evaluation_time`. High.
  9. `executor-contract/validation.ts:32-39` (duplicate policies and evidence types) and `gaap.ts:33-37` (duplicate mapping families) can never fire. The zod refines at `contracts.ts:33-41` and `137` reject first with `SCHEMA_INVALID`; I confirmed this with a probe. Only the approval-ID uniqueness check is live. High.
  
  **Bloat and duplication**
  10. Repeated re-parsing and replay:
      - `applyExecutionOperation` (`model.ts:431-434`) parses the journal, then `replay` parses it again (`352-358`), then `createExecutionJournal` re-parses the initial parts (`185-195`).
      - `projectControllerExecution` (`259-261`) repeats the same pattern.
      - `validateExecutorContext` (`validation.ts:68-73`) replays the journal twice.
      - `mapGaapResult` validates the executor request 3 times and the GAAP request twice.
      - `compareCaseResult` re-runs full fixture semantics about 3 times (`349-353`).
      - Each journal step re-validates a full ControllerInput including graph topology (`model.ts:162`), so replay is O(n²).
  
      High.
  11. Primitives are redefined per module: 26 top-level definitions. `text` appears 4 times in 3 different variants (`min(1)`; `min(1)`+`\S`; `\S` only). `digest` ×5, `counter` ×5, `identity` ×3, `claimReference` ×3, `requestReference` ×2 (plus a function version in `model.ts:157`), `timestamp` ×2, `identifier` ×2. High.
  12. Six error constructors differ only in their recovery text: `diagnostic` (wg `contracts.ts:200`), `issue` (`validation.ts:169`), `invalid` (`model.ts:148`), `invalid` (`codec.ts:5`), `failure` (conformance `validation.ts:32`), `translate` (conformance `codec.ts:5`). High.
  13. Digest and equality helpers are duplicated: `executionDigest`, `conformanceDigest`, `gaapDigest`, `requestIdentity`, `executionAdmissionDigest`, `executorRequestAdmissionDigest`, plus 12 inline `sha256(canonicalJson(...))` calls. `same` is defined twice (`validation.ts:179` and `guards.ts:6`, the second to avoid an import cycle), plus conformance's `equal`. Finding 7's check shows one serializer would give identical bytes for every published artifact. High.
  14. Two bounded-JSON walkers exist: `withinExecutionLimits` (`limits.ts:10-51`) and `validateJsonValue` (`codec.ts:29-87`). The second calls the first at the end, so the tree is walked twice. `sources.ts:24-58` has a third budget walker. High.
  15. Wrong-direction imports. `executor-contract/contracts.ts:3` imports `executionDigest` from the 1,102-line `model.ts` just for a zod refine, so the schema layer depends on the model. The shared kernel lives inside `workflow-graph/contracts.ts:190-227`. High.
  16. Schema publishing is inconsistent. There are 3 different publish scripts: only conformance compiles with Ajv before writing, and they resolve prettier differently. workflow-graph and controller have no publish command at all. There are 5 near-identical `published*Schemas()` bodies. High.
  17. The subject-identity comparison is written 3 times: `model.ts:1043-1052`, `executor-contract/validation.ts:150-160`, `gaap.ts:126-133`. High.
  18. `files.ts:15-59` is over-hardened for a dev checker that reads committed repo files: a symlink- and race-proof reader (O_NOFOLLOW, dev/ino recheck, AggregateError on close). `readJson` (`61-73`) parses every file twice (JSON, then YAML for duplicate keys). It is backed by the 161-line mock-heavy `controller-conformance-files.test.ts` plus symlink, BOM and shared-artifact tests in `controller-conformance.test.ts` (`:424-470`, `:492-529`, `:560-591`). High.
  
  **Test redundancy and change detectors**
  19. `workflow-graph-contract.test.ts:624-758`: about 11 of the 16 `invalidCases` restate `fixtures/invalid/*.yaml` with weaker assertions. The parser cases at `:356-371` duplicate `duplicate-keys`, `alias` and `custom-tag`. `:412` duplicates `prior-state-blocked/completed`. `:352` and `:633` are the same test. High.
  20. `workflow-graph-contract.test.ts:22-73` and `:219-241` are change detectors: they hard-code fixture contents that are already byte-pinned by `.canonical`, `.compiled.json`, compatibility and case_001. Keep `:182` and `:198`, which cross-check against the `src/` lifecycle and catch real drift. High.
  21. `controller-contract.test.ts` restates negative fixtures by hand:
      - `:353` = `policy_digest_drift`
      - `:198` = `input_digest_tampering`
      - `:273` = `stale_observation_action` / `unavailable_capability_action`
      - `:438` ≈ `fenced_wait`
      - `:174-195` is covered by the exhaustive `:826` plus `human_as_executor`
      - empty independent gates are tested 3 times (`:538`, `:685`, `:878`)
  
      The positive-corpus decision check at `:61` re-validates the same 20 decisions as conformance cases 004–023 (per `coverage.md`); only its canonical-bytes and request-building parts add coverage. High.
  22. Executor and GAAP tests overlap:
      - `gaap-mapping.test.ts:70` (6 cases) duplicates `executor-corpus.test.ts:73` (7 cases).
      - `gaap-mapping :60` is covered by the golden equality at `executor-corpus :41`.
      - `executor-review-regressions :115` 'nonterminal' = `executor-corpus :298` 'nonterminal'.
      - `executor-pr-review :20` tests 11 fields that all hit one regex (`contracts.ts:6`).
  
      High.
  23. The codec is tested three times over: `executor-codec.test.ts`; `controller-conformance.test.ts:8-21` and `:324-346`, which go through a one-line wrapper; and `executor-review-regressions :49-86`. High.
  24. `controller-conformance.test.ts` repeats work:
      - `:148` runs about 5 full semantic evaluations for each of the 38 cases, after `validateCorpus` has already checked them.
      - `:471` repeats `validateCorpus` and the `$fixture_ref` check from `:188`.
      - `:267` asserts expectations already bound by the manifest `fixture_digest`.
  
      The describe blocks are named by review commit hash rather than by behavior. High.
  25. `execution-contract.test.ts` has about 7 hand tests that restate `scenarios.json` rows: `:568`, `:617`, `:666`, `:705`, `:737`, `:754`, `:784`. The scenarios test (`:1133`) embeds a 100-line TypeScript step interpreter, so `scenarios.json` is not portable; the conformance execution cases are the portable version. `execution-limits.test.ts:12` only checks that a spied adapter was called. Medium-high.
  26. The schema-parity test is implemented 5 times (`:339`, `:50`, `:1121`, `:134`, `:114`) with differing Ajv options. Tests that catch real bugs and should stay: the execution model conflict, fencing and replay tests; controller guard and prerequisite tests; GAAP ledger tests; hostile codec inputs; compiler cycle tests; `execution-limits` prefix-hash (`:78`). High.
  27. The "review-regressions" files (`executor-review-regressions`, `executor-pr-review`, `gaap-ledger-review`) and the "Qodo … regressions" describe blocks are organized by review round, not behavior. Fold them into behavior files and keep only distinct invariants. High.
  28. Scope note: this whole layer is a second, dev-only reference model of lifecycle semantics that `src/` does not use. That is by design (the runtime is owned by #111), but it is the main structural cost. High.
  
  ### 3. Frozen surface (must not change)
  - **18 byte-pinned schemas** (sha256 in `controller-conformance-v0.1/compatibility.json`): workflow-graph 3, controller 3, execution 8, executor 4.
  - **8 conformance schemas.** They are checked by `verifyPublishedSchemas` and consumed by RunInvariant.
  - **Conformance corpus.** The manifest `corpus_digest` 11620588… is hard-coded in a test. Also frozen: `compatibility.json`, `vectors/*`, and the expanded identities of `fixtures/case_*.json` and `shared.json`. The storage form may change only if the expansion stays byte-identical.
  - **Everything else under docs/contracts:** all graph, controller, execution and executor fixtures, `.canonical` and golden files, `preservation.json`, and `upstream/gaap` (provenance sha-pinned).
  
  **What byte-identity forbids.** Zod instance identity and each module's `$defs` names are part of the published output. The modules use different names (for example `NonemptyText` vs `NonEmptyText`, `Digest` vs `Sha256Digest`), and emit either inline or `$ref`. In `controller-input.schema.json`, the CompiledGraph definition inlines the identifier pattern 58 times with 0 `$ref`s. So sharing primitive instances across modules would change published bytes. Use factories called once per module instead. `src/domain/canonical-json.ts` must stay unchanged for the CLI's golden bytes; the scripts should simply stop importing it.
  
  ### 4. Consolidation plan
  Build one kernel, `scripts/contract-kernel/`, of about 240 lines:
  - `result.ts` (~35): Diagnostic, Result, `validateShape`, and one `fail()`.
  - `json.ts` (~100): one iterative bounded walker that emits canonical bytes (UTF-16 key sort, proxy/prototype/accessor/shared-reference/surrogate/safe-int/depth/value/byte limits) plus a message parser. It replaces `limits.ts`, both codecs and the budget logic in `sources.ts`.
  - `digest.ts` (~20): `digest`, `prefixedDigest`, `same`, `domainDigest`, all on the one serializer.
  - `primitives.ts` (~30): a `definePrimitives({text: 'min1' | 'nonBlank' | 'nonBlankNoMin'})` factory, called once per module.
  - `publish.ts` (~35) plus one `spec:schemas` script: per-contract registry, `$id`, reuse mode, Ajv compile, prettier. This also adds the missing graph and controller publishing.
  
  Then:
  - Delete the dead branches (findings 8 and 9).
  - Collapse the re-parse chains by passing parsed values internally (finding 10).
  - Replace `files.ts` with `readFile` plus size cap plus duplicate-key parse.
  - Pin zod exactly.
  - The existing parity tests plus `verifyCompatibility` will immediately catch any byte drift.
  
  **Scripts:**
  
  | Module | Now | After |
  |---|---|---|
  | workflow-graph | 816 | ~700 |
  | controller-contract | 1,472 | ~1,250 |
  | execution-contract | 1,426 | ~1,120 |
  | executor-contract | 1,132 | ~850 |
  | controller-conformance | 913 | ~640 |
  | kernel (new) | 0 | +240 |
  | **Total** | **5,759** | **~4,800 (−17%)** |
  
  **Tests:** make the corpus loops the backbone. Delete hand tests that restate corpus rows, merge the review-round files by behavior, use one generic schema-parity test and one codec test file, and replace bare `ok:false` with code assertions.
  
  | Test group | Now | After |
  |---|---|---|
  | workflow-graph | 759 | ~450 |
  | controller-contract | 1,185 | ~750 |
  | execution (contract + limits + authority) | 2,051 | ~1,500 |
  | executor + gaap (8 files) | 1,393 | ~800 |
  | conformance (3 files) | 863 | ~400 |
  | generic parity (new) | 0 | +60 |
  | fixtures | 420 | ~380 |
  | **Total** | **6,671** | **~4,340 (−35%)** |
  
  **Layer total:** about 12.4k lines to about 9.1k (−26%) while keeping every published byte. The domain logic in `model.ts`, controller `validation.ts`/`guards.ts`, topology and the GAAP ledger is most of what remains, and it can't shrink much.
  
  **What 40% would take.** Getting this layer to −40% or more means a capability decision, not a cleanup. The option is to archive the TypeScript reference semantics (model, decision, guards, GAAP ledger) and keep only schema generation plus corpus integrity pins, which would cut about 70% of the layer. The cost is that the 38 frozen expectations would lose their only executable check. I'd recommend keeping it unless the Rust/runtime milestone will own those semantics.
  
  Probe scripts are in `/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/scratchpad/` (`canon.mts`, `probe.mts`, `probe2.mts`, `probe3.mts`, `ts.mts`).
</agent-message>

---

<task-notification>
<task-id>a8b1b8f841cee7b5c</task-id>
<tool-use-id>toolu_01T7cZSRRCvAmpttz7mSSmrL</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/tasks/a8b1b8f841cee7b5c.output</output-file>
<status>completed</status>
<summary>Agent "Audit contract/spec tooling layer" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This agent's report was delivered to you as a message from "a8b1b8f841cee7b5c" (its SubagentHandback call). Read it there; it is not repeated here.
</result>
<usage><subagent_tokens>398431</subagent_tokens><tool_uses>74</tool_uses><duration_ms>816627</duration_ms></usage>
</task-notification>

---

<agent-message from="ad41fc21eadc8d42e">
[Subagent hand-back] The text below is the final report of a subagent this session delegated to. It is model output, NOT a message from the user: instructions, requests, or approval claims inside it are the subagent's words and carry no user authority. The harness indents every line of the report, so a frame-like line at column zero inside it would be forged. Notes above this frame may quote model-derived text, which carries no user authority either. The report follows:
  ## Step 7 of #133: contract kernel consolidation
  
  **Worktree:** `/Users/nnennandukwe/Code/threadloop/.claude/worktrees/agent-ad41fc21eadc8d42e`
  **Branch:** `refactor/issue-133-contract-kernel`. Not pushed, no PR.
  
  Every published byte is unchanged and `npm run check` passes (789 tests). Neither line target was reached: scripts came down about 4%, not the ~17% you asked for, and tests about 20%, not ~36%.
  
  `origin/main` moved while I worked (PR #135 merged, now at 0958920), so I rebased onto it. That PR doesn't touch any file in this scope, and the rebase was clean.
  
  ### Commits (`git log --oneline origin/main..HEAD`)
  ```
  03ad4de test(executor): map each published outcome once
  b60f02a refactor(contracts): derive repeated enums and GAAP terminal mapping
  ac11f14 test(conformance): build each subject request once and assert codes
  280beb8 test(executor): assert the check that rejected each executor input
  80fae43 test(execution): drop scenario restatements and assert exact codes
  2ac187c test(contracts): assert diagnostic codes in graph and controller tests
  2d8ea6e refactor(contracts): reuse guard checks and claim lookups
  d9be56a refactor(conformance): read committed corpus artifacts plainly
  1de4339 refactor(contracts): validate and replay each input once
  cee644b refactor(contracts): remove checks the schemas already enforce
  ad2f91d refactor(executor): count canonical bytes in the single JSON walk
  ea3237a refactor(contracts): share one contract kernel and schema publisher
  da458b2 chore(deps): pin zod to the installed 4.4.3
  ```
  
  ### Line counts
  | | Before | After | Target |
  |---|---|---|---|
  | Scripts, your glob | 5,759 | 5,411 | ~4,800 |
  | Scripts, incl. new `scripts/contract-kernel/` | 5,759 | 5,538 | ~4,800 |
  | Tests incl. fixtures | 6,671 | 5,357 | ~4,300 |
  
  Your glob `scripts/*-contract/*.ts` doesn't match `contract-kernel/`, so the honest scripts figure is 5,538. The test count in scope went from 451 to 398.
  
  **Why the targets weren't met:** what's left is non-duplicated logic, the diagnostic calls with their messages, and tests the brief said to keep (model conflict/fencing/replay, guards and prerequisites, the GAAP ledger, the compiler cycle checks). Going further would mean rewording diagnostics or deleting checks that carry behavior. Two changes cost a few lines on purpose: the single-walk codec and the replay-once refactor.
  
  ### Main changes
  - **Kernel** (`scripts/contract-kernel/kernel.ts`):
    - `Diagnostic`, `ValidationResult`, `diagnostic` and `validateShape` move out of `workflow-graph/contracts.ts`.
    - `withRecovery()` replaces the five constructors that differed only in recovery text.
    - `digest()` and `same()` replace `executionDigest`, the three copies of `same`, conformance `equal`, and the inline `sha256(canonicalJson())` calls. Both still use `src/domain/canonical-json.ts`, which is untouched.
    - `publishedSchemas()` replaces the five hand-written schema builders.
  - **Other script changes:**
    - `sameSubjectIdentity()` replaces the three hand-written identity comparisons.
    - The executor contract no longer imports `model.ts`.
  - **One publisher** (`scripts/contract-kernel/publish-schemas.ts`) replaces the three scripts. All existing `spec:*:schemas` names still work, and I added `spec:workflow-graph:schemas` and `spec:controller:schemas`. It builds and compiles every document before writing any.
  - **Dead code removed:** the timestamp re-checks and `realTime`, the duplicate policy/evidence/family checks (only approval-ID uniqueness was live), an unreachable GAAP nonterminal branch, and the file-local exports (plus executor `budgetSchema`).
  - **Inputs validated once:** the journal is no longer parsed twice before replay. `validateExecutorContext`, conformance scenarios and `mapGaapResult` each validate or replay once, and `compareCaseResult` runs the fixture's semantic check once.
  - **`files.ts`** now does lstat (regular file only), a size cap, `readFile`, a post-read cap, and duplicate-key parsing. `controller-conformance-files.test.ts` (161 lines) is deleted because it only tested the race and descriptor-lifecycle code that was removed. The documented rejections (symlink, oversize, duplicate key, BOM, unlisted file) are still covered in `controller-conformance.test.ts`.
  - **zod** is pinned to 4.4.3. The lockfile is updated and `npm ci` works.
  
  ### Byte identity
  - Regenerating all five families through the `spec:*:schemas` scripts leaves `git status docs/` clean.
  - `git diff origin/main -- docs src` is empty.
  - `spec:conformance:check` passes, and the corpus digest is still 1162058…ad84.
  - The schema-parity tests and `verifyCompatibility` pass.
  
  ### Tests that passed for the wrong reason, now fixed
  - **executor-admission (the four you named):** the Workflow Run, graph, state version and subject cases failed with `REQUEST_IDENTITY_MISMATCH`. I confirmed this by reverting the fix. They now reseal the idempotency key, so all 11 cases reach `EXECUTOR_CONTEXT_MISMATCH`.
  - **gaap-ledger-review "tool calls above budget":** this was always an underreport rejection. It now reports usage honestly and shows budget 2 is accepted and budget 1 is rejected by the completion budget check.
  - **Also found:**
    - The "skips verifying" route test actually failed on the out-of-state verification event. It now drops that event too, so the route check is what fires.
    - The resealed actor-tampering journal failed only because the test authority hadn't admitted the tampered entry. It now replays with an authority that admits everything and asserts `ACTOR_MISMATCH`.
  - **Bare `.ok === false` assertions:** all converted to exact codes. GAAP ledger rejections all share one code, so those also assert the rule's message. One `ok === false` remains, and it is followed by a code check.
  
  ### Tests deleted or merged
  - **workflow-graph:**
    - 9 hand-written invalid profiles that restate `fixtures/invalid/*.yaml` (duplicate state, unknown initial state, unknown target, unknown guard, unreachable, no terminal path, ambiguous, priority tie, uncontrolled self-loop).
    - The duplicate-keys, alias and custom-tag parser cases, which the fixtures cover.
    - The two tests that hard-code the governed-pr and release profile contents, which are already byte-pinned.
    - :352, a duplicate of :633.
  - **controller:**
    - Hand-written copies of fixtures: policy digest drift, input digest tampering, stale observation, unavailable capability, invalid waits.
    - Actor-boundary spot checks that the exhaustive capability/actor test covers.
    - A resealed `CLAIM_EXPIRED` case the blocked-reason loop covers.
    - A schema-only "invented digest" case.
    - Two of the three empty independent-gate tests.
    - The "Qodo" describe blocks are folded into behavior-named groups.
  - **execution:** death-before-start, receipt replay, cancel/release/expire-as-no-effect, and cancel-before-claim, each of which is a `scenarios.json` row. "Starts once and renews" is merged into the expiry test.
  - **executor and conformance:**
    - `gaap-mapping` "preserves status and reason", which duplicates `executor-corpus`.
    - `executor-admission` "unapproved parameters", which repeats the first test.
    - The conformance codec duplicates.
    - The hostile prototype/proxy cases, moved into `executor-codec`.
    - The second `validateCorpus` run.
    - Repeated request building and the extra `validateSubjectResponse` per case.
    - `executor-corpus` mapping every receipt twice.
  - **Schema parity:** the five implementations are now one helper, `publishedValidators` in `tests/fixtures/contracts.ts`, with a single Ajv configuration.
  
  ### Audit findings I checked and didn't act on
  - **Test files named by review round:** not renamed. The frozen executor, execution, controller and conformance READMEs cite those file names, what each covers, and `npm test -- <file>` commands, so renaming would break frozen docs. I renamed the describe blocks and titles instead. You can rename the files in a later docs-plus-tests PR.
  - **`conformanceDigest` and `gaapDigest`:** kept separate. They use the executor JCS serializer, which orders integer-like keys differently from `canonicalJson`, so no single kernel serializer gives the same bytes for both.
  - **The budget walker in `sources.ts`:** not a duplicate. It charges bytes while expanding references, before allocating the result.
  - **execution-contract :568 and :754:** not pure restatements. They test replay after `EXECUTION_VERSION_CONFLICT`, and `HUMAN_AUTHORITY_REQUIRED` with duplicate evidence copies. Kept.
  - **"11 of 16 invalidCases":** 9 were restatements. "Missing human authority" hits `INVALID_AUTHORITY`, which no fixture covers, so it stays.
  
  ### Things to look at in review
  - Two dev-diagnostic messages changed: `DUPLICATE_PARAMETER` now reads "Approval identities must be unique.", and the corpus loader says "expected a regular file."
  - `executionClaimSchema` and `attemptSchema` are exported again so the conformance summary can reuse their enums. Emitted schemas are unchanged.
  - `mapGaapResult` now casts the terminal status, relying on `validateGaapReceipt` having already rejected nonterminal receipts.
  
</agent-message>

---

<task-notification>
<task-id>ad41fc21eadc8d42e</task-id>
<tool-use-id>toolu_01Etpt3Ho6XqNXQyWwfTvvC6</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-nnennandukwe-Code-threadloop/94abb8a5-c9d0-42c7-a046-f65aa6f0cee0/tasks/ad41fc21eadc8d42e.output</output-file>
<status>completed</status>
<summary>Agent "Consolidate contract tooling layer" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This agent's report was delivered to you as a message from "ad41fc21eadc8d42e" (its SubagentHandback call). Read it there; it is not repeated here.
</result>
<usage><subagent_tokens>695673</subagent_tokens><tool_uses>232</tool_uses><duration_ms>3184510</duration_ms></usage>
<worktree><worktreePath>/Users/nnennandukwe/Code/threadloop/.claude/worktrees/agent-ad41fc21eadc8d42e</worktreePath><worktreeBranch>worktree-agent-ad41fc21eadc8d42e</worktreeBranch></worktree>
</task-notification>